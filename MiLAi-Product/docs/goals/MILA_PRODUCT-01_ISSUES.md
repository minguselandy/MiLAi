# MiLA Product-01 compact issue log

| ID | Stage | Classification | Status | Root cause and disposition |
| --- | --- | --- | --- | --- |
| P01-I001 | S0 | Product CI scope | RESOLVED | Frozen architecture refresh test requires legacy project Goal files intentionally excluded from Product. Product CI now runs all bundle-applicable tests and explicitly omits exactly that one legacy-only refresh test; `architecture/v1.0` bytes remain unchanged. |
| P01-I002 | S0 | Runtime receipt compatibility | RESOLVED | `MemoryResolveService` preferred ephemeral Evidence receipt v0.2 over the existing durable Canonical receipt v0.1, breaking same-call receipt reuse in four PostgreSQL tests. Canonical-only API outcomes now use the governed durable receipt; Raw Evidence outcomes retain non-canonical v0.2 semantics. |
| P01-I003 | S0 | Operations smoke semantics | RESOLVED | L1 smoke queried only an opaque random marker; retrieval found one governed result but `DecisionEngine` correctly abstained as `QUERY_AMBIGUOUS`. The smoke now asks an explicit subject/relation lookup and reuses that exact query for the post-revocation negative control. |
| P01-I004 | S0/S1 | Adapter identity propagation | RESOLVED | Typed client, `AgentMemory`, AutoGen, LangGraph and OpenWorker capture paths dropped authoritative `speaker/source_context`, leaving capable captures identity-unknown. They now propagate the existing source-context shape only when supplied; no ordinal is inferred and legacy capture remains valid. |
| P01-I005 | S0 | OpenWorker MCP frame boundary | RESOLVED | The UDS client limited the whole JSON-RPC frame to the MCP server's 64 KiB structured-result ceiling, ignoring JSON-RPC plus compatibility-text overhead. Its bounded default now matches the existing 2 MiB Host response ceiling; caller overrides remain capped at 2 MiB. |
| P01-I006 | S1 | Terminal context trace | RESOLVED | Budget-infeasible context text had an exact digest but omitted serialization-part offsets. The Reader-visible trace now enumerates the five actual terminal parts with exact character/UTF-8 ranges and hashes while correctly admitting zero Evidence units. |
| P01-I007 | S1 | Preflight readiness protocol | RESOLVED | The first harness omitted Jinja needed by the frozen Qwen chat template; a later repair requested a readiness timeout above the public 30-second schema cap. Startup now proves exact tokenizer/template readiness, and projection lag uses at most four explicit 30-second attempts while preserving timeout as a protocol/infrastructure failure. |
| P01-I008 | S1 | Outcome-blind corpus shape | RESOLVED | Two selected records contained a duplicate source session ID or one empty turn. Duplicate instances now receive a deterministic occurrence discriminator before identity hashing; empty turns are omitted while material turns retain original ordinals and adjacency. No question, answer, gold text, or Product outcome informed the repair. |
| P01-I009 | S2 | Accepted-state decision boundary | RESOLVED | An existing acquisition state with an empty accepted-reference set was treated as absent, allowing governed candidates to be promoted into accepted Binding references. The decision path now falls back to governed candidates only when acquisition state is absent; an empty accepted set remains empty, with a targeted regression test. |
| P01-I010 | S3 | Typed terminal scoring | RESOLVED | The S3 scorer treated correct `ABSENT / NO_CANDIDATE` as a failed insufficiency scenario because it recognized only older `MISS/ABSTAINED` names. The typed terminal set now includes `ABSENT`; a fresh database rerun increased honest task success from 22/24 to 23/24 without changing Product behavior. |
| P01-I011 | S3 | Formation regression contract | RESOLVED | A legacy canary test equated a newly governed Formation candidate with an accepted semantic Binding. S2 deliberately separated these layers. The test now proves one governed candidate and zero accepted Bindings for its relation-ungrounded query; the fresh full Runtime matrix passed 743 tests with one declared optional skip. |
| P01-I012 | S4 | Raw Evidence completion authority | RESOLVED | The repaired 128-case run found three B0 and one B1 strict Wrong COMPLETE decisions on multi-session questions. `DecisionEngine` treated one grounded Raw Evidence lookup Binding as proof that the whole answer was complete, although Product owns context rather than a generative Reader. Raw Evidence bindings remain accepted, source-ready recall, but no longer independently grant COMPLETE; only closed Canonical or executed-operator proof may do so. A regression test preserves the accepted grounded relation while requiring `PARTIAL`. |
