# DG-05 / LC-009 验收证据

> 日期：`2026-08-15`（Asia/Shanghai）  
> Goal：`DG-05 冲突保留与 OpenIssue 治理`  
> 结论：`PASS — ACHIEVED FOR 0.1.x EXPERIMENTAL`  
> 冻结结论：`NO-GO FOR SCHEMA FREEZE`

## 治理闭环

冻结的 synthetic Python 版本用例已经从真实 HTTP API 回放：

```text
E1 deployment says Python 3.11
→ APPROVE CREATE
→ M1 V1 ACTION_SAFE / EFFECTIVE

E2 pyproject says >=3.12
→ APPROVE CONTRADICT
→ same Head V1
→ OpenIssue(CONFLICT, OPEN) with SUPPORT_BRANCH + CONTRADICT_BRANCH

E3 CI + production report 3.12
→ SUPERSEDE proposal with expected_issue_revision
→ two RESOLUTION_CANDIDATE relations
→ READY_FOR_REVIEW
→ explicit USER APPROVE after deterministic discharge validation
→ M1 V2 + same OpenIssue RESOLVED

revoke one used E3 Evidence
→ V2 GroundingBlock
→ same OpenIssue ID WAITING_EVIDENCE
→ append-only RESOLUTION_EVIDENCE_REVOKED transition
```

Discharge 在 proposal 创建与 review 时都检查：目标 Claim、issue revision CAS、合法状态、
required authority、required scope、实际 branch 存在、显式 addressed branches、Evidence 类型、
admissibility 和独立 source 数量。模型输出、摘要、检索遗漏和时间经过没有任何关闭入口。

## 负向与并发证据

- 两个候选用相同 issue revision 并发，仅一个进入 `READY_FOR_REVIEW`；另一个返回
  `ISSUE_REVISION_CONFLICT`；
- resolution 被 REJECT 时，同一 issue 通过 revision CAS 回到 `OPEN`；
- `CONTRADICT` 不创建 ClaimVersion/VersionTransition，不移动 Head；
- branches 与 resolution candidates 均为结构化 `grounding_relation`；
- 每次状态变化生成 append-only `open_issue_transition`；
- 撤销已采用的 resolution Evidence 后重开同一 ID；
- API issue detail 返回 branches、discharge rule 和完整 transition trace。

## Artifact hashes

| Artifact | SHA-256 |
| --- | --- |
| OpenIssue/transaction migration | `3e13d34c241c2616790216debd7b21b8a3b5b64bd11f4a8f22c6c26c42c041f2` |
| TX-05 reopen migration | `7b6fec9c29cf8dee2b9c64dc682ec6a84e5545ea36cb8c50b05930a7a93f5764` |
| API E1→E2→E3 tests | `69eabb5ee4ed59f5f87a50d74c63da450b9bf3fc9e1aaf37b1e46e90489fb025` |
| CAS/concurrency tests | `c7019183048dd0967c1349ad0eaae841b87f76af913d821f8998e1e5d36da741` |

## Gate 边界

本报告证明 OpenIssue identity、branch、CAS、discharge 与 reopen。stale FTS/vector candidate 的
拒绝属于 DG-07 Canonical Gate，异步物理清理属于 DG-06 后续工作；两者尚未因此被宣称完成。
