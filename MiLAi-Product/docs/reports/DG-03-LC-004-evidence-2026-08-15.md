# DG-03 / LC-004 验收证据

> 日期：`2026-08-15`（Asia/Shanghai）  
> Goal：`DG-03 Evidence Plane 与幂等摄取`  
> 结论：`PASS — ACHIEVED FOR 0.1.x EXPERIMENTAL`  
> 冻结结论：`NO-GO FOR SCHEMA FREEZE`

## 已交付

- tenant-scoped `content_blob`、`evidence_record`、`idempotency_record`、`outbox_event`；
- 四张表全部启用并强制 RLS；
- Security Definer `milai.tx01_ingest_evidence`，API 只有 EXECUTE/SELECT 最小权限；
- 同事务写 Evidence、OperationalEvent、Outbox 和 idempotency response；
- 本地 SHA-256 content-addressed blob adapter，原子落盘、权限和读取时 hash/length 复验；
- `POST /v1/evidence`、GET、lineage，以及 local bearer authentication；
- tenant 来自认证 settings；body tenant 只能一致，不能切换 tenant；
- capture identity/content identity trigger 防止原地覆盖。

## 关键证据

```text
Full suite                         31 passed
TX-01 same-key replay             same Evidence/Outbox IDs, replayed=true
TX-01 fingerprint conflict        HTTP 409 IDEMPOTENCY_CONFLICT
TX-01 concurrent same key         one canonical side effect
TX-01 injected failure            idempotency/Evidence/Outbox all rolled back
same tenant same bytes            distinct Evidence IDs, shared Blob ID
different tenants same bytes      different Blob IDs
capture identity owner UPDATE     IMMUTABLE_EVIDENCE_IDENTITY
unauthenticated/wrong token       AUTHENTICATION_REQUIRED
body tenant switch                TENANT_MISMATCH
```

Database inspection confirmed Alembic head `0003_evidence_plane` and forced RLS on all four
new tables. `milai_api` has no direct INSERT/UPDATE/DELETE on Evidence Plane tables; TX-01 is
the only ingest mutation entrypoint.

## Artifact hashes

| Artifact | SHA-256 |
| --- | --- |
| Migration `0003` | `89da2aeee8aab20dcfa1f917eae18457f786d86492a288573a6e47aa12250ae3` |
| Blob adapter | `cf200432ac11bd5f05a6d26cfdba0be7115a5827120e6573f7b3cc7c5d6fcd70` |
| Evidence application service | `46662a62443513ea8c525af4cfc6c8c63d6eb0ad0f087c03df091f948e8e45f7` |
| Evidence repository | `41bde79c82c17099ba63395a7e160e8af484743b0f2c7af2bab093901e11f50a` |

## 保留边界

- Evidence ingest 暂不分配 `canonical_commit_seq`，该覆盖范围仍是 frozen crosswalk 的
  open decision；Outbox 字段保持 nullable；
- Blob 先落盘、DB 后提交。崩溃可能留下不可达 orphan blob，后续 deletion reconciliation
  负责安全清理；失败不会生成可读 Evidence；
- 本报告生成时 Lineage 尚未接入 canonical refs；DG-04 完成后已改为从受 RLS 保护的
  `grounding_relation` 返回真实 ClaimVersion/OpenIssue relation，见 DG-04/DG-05 报告；
- 本报告不证明 Claim、OpenIssue、revoke、projection 或 `LG-02`。
