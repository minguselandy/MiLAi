# DG-01 / LG-01 验收证据

> 日期：`2026-08-15`（Asia/Shanghai）  
> Goal：`DG-01 可重复 Runtime 基础`  
> Gate：`LG-01 Runtime`  
> 结论：`PASS — ACHIEVED FOR 0.1.x EXPERIMENTAL`  
> 冻结结论：`NO-GO FOR SCHEMA FREEZE`

## 验收环境

```text
Host Python: 3.10.12
Locked Runtime Python: CPython 3.11.13
uv: 0.8.3
Docker client/server: 28.2.2 / 28.2.2
PostgreSQL client: 16.14
Database image: pgvector/pgvector:pg16
Default host bindings: PostgreSQL 127.0.0.1:15432, API 127.0.0.1:18080
```

测试使用本次创建的专用 `milai-lean-v1` Docker volume 和 synthetic tenant/event 数据，
没有导入个人数据。

## Artifact 校验

| Artifact | SHA-256 |
| --- | --- |
| `runtime/pyproject.toml` | `37a663f603eedd2972064a806df6e4c4b50e6e3afee732ae2c8128f283fad067` |
| `runtime/uv.lock` | `4f12bca74846302e734cc2d03f3f3f0573ad2f209c4332fa1d850eeae85d3f4b` |
| `runtime/compose.yaml` | `2d9a46b53912d961f1ef975895de035c6774e705b510b03ed23534ea3de6cb3b` |
| `runtime/docker/initdb/010_roles.sh` | `38094279bef383a8d7adfabe46b417962380aab91e157a7c6087f176d5ab2593` |
| Migration `0001` | `7b227cfc567616194008fd0e38467d7ad9f9d0728dc7e31ddac74b1f530da6b5` |
| Migration `0002` | `af088d901b637487789c9b56c0881511b5d74675005d965e6140dbb29d24b71a` |

Migration `0002` 属于同步推进的 DG-02 基础，不改变 DG-01 Gate 判定。后续变更会产生新的
manifest，不回写本次 hash。

## 需求到证据

| DG-01 关键结果 | 证据 | 结果 |
| --- | --- | --- |
| Runtime package 与锁文件 | `pyproject.toml`、`uv.lock`、Python 3.11 `.venv` | PASS |
| PostgreSQL/pgvector Compose | 新专用 volume 启动，health 为 healthy，loopback 映射 | PASS |
| typed settings fail-fast | missing、unknown、remote bind、broad blob root tests | PASS |
| migration 可重复 | fresh upgrade；`downgrade base → upgrade head` 成功 | PASS |
| 真实 PostgreSQL fixture | API login role 的真实 connection/pool tests | PASS |
| live/readiness 区分 | `/health/live` 与 `/health/ready` contract + 真实 curl | PASS |
| 质量命令 | Ruff、mypy、pytest 均成功 | PASS |
| 本地 runbook | `docs/runbooks/local-runtime.md` | PASS |
| 外部依赖隔离 | AST import boundary test | PASS |
| API/worker 可启动 | Waitress 实际监听；worker `--once` 实际运行 | PASS |

## 实际执行结果

```text
uv lock --python 3.11                         PASS
uv sync --frozen --dev                        PASS
uv run ruff format --check .                  PASS (25 files)
uv run ruff check .                           PASS
uv run mypy src                               PASS (14 source files)
uv run pytest                                 PASS (20 tests)
Alembic fresh upgrade                         PASS
Alembic downgrade base → upgrade head         PASS
PostgreSQL role/RLS subset                    PASS (6 real DB tests)
milai-db-check                                canonical database: ready
milai-worker --once                           foundation_worker_ready
GET /health/live                              HTTP 200
GET /health/ready                             HTTP 200
```

API liveness 返回并保留：

```text
schema_status = 0.1.x EXPERIMENTAL
implementation_status = CANDIDATE
schema_freeze = NO-GO FOR SCHEMA FREEZE
```

## 直接依赖许可证检查

| Package | Locked version | Metadata license |
| --- | --- | --- |
| alembic | 1.19.1 | MIT |
| flask | 3.1.3 | BSD-3-Clause |
| psycopg / binary | 3.3.4 | LGPL-3.0-only |
| psycopg-pool | 3.3.1 | LGPL-3.0-only |
| pydantic-settings | 2.15.0 | MIT |
| sqlalchemy | 2.0.52 | MIT |
| waitress | 3.0.2 | ZPL 2.1 |
| pytest / pytest-cov | 8.4.2 / 6.3.0 | MIT |
| ruff | 0.16.3 | MIT |
| mypy | 1.20.2 | MIT |
| types-waitress | 0.1.9 | Apache-2.0 |

完整传递依赖版本以 `uv.lock` 为准。依赖 metadata 审计没有发现缺失的直接运行依赖
license 字段；build-only 依赖由 lockfile 固定，发布打包前仍需生成最终 SBOM/license bundle。

## 安全与隐私检查

- `.env` 被忽略且未创建；测试密码只通过进程环境传入专用本地容器；
- 仓库扫描未发现本次 dev-only 数据库口令；
- API 配置摘要和错误不显示 DSN；
- API 和数据库只暴露 loopback；
- API、worker 和 migration 使用不同 DSN/角色；
- Runtime source 没有导入 ReMe、hindsight、graphiti、Mem0、MemoryIntention 或 OpenViking。

## 尚未证明

- Frozen Architecture `1.0.0` crosswalk：artifact 仍缺失，`LG-00` 未通过；
- Canonical TX-01～TX-06、Claim/OpenIssue、删除和检索：属于后续 Goal；
- 在第二台物理设备上的重现：尚未执行；
- Production readiness：不在当前范围且禁止宣称；
- 最终 SBOM、签名发布包和性能 SLA：Local Beta 前处理。

因此本报告只证明 `DG-01 / LG-01`，不证明 `LG-02` 或更高门禁。

