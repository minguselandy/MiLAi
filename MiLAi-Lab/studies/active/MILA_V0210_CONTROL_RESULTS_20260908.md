# V02-10：真实 Control State 调用与负控终态

状态：**COMPLETED_DIAGNOSED_KEEP_BASELINE**。
已按 Goal §9 的“某门得到足够否定/诊断”路径收口，保留 MCP 0.1.15 / A0。
这不是“复杂任务已解决”或“简单基线全部通过”：首次困难交付三组均失败，
Control State 的小幅 token 节省伴随不当恢复建议，缺少进入 P2/E2 的质量信号。
P2/E2、P3/E3 均 **NOT_ENTERED**，关闭本轮扩张，不为挽救失败继续调提示或重跑旧题。

## 执行与边界

用户原始“详细阅读并执行、遇到权限直接通过、人工审查用 subagent review”已授权
文档范围内的有界本地工作。此前再次询问 B1 授权属于多余确认，本次直接继续，
在生成前分别登记 B1 六次 / 64,000 raw / 600 秒、B2 四次 / 32,000 raw / 300 秒。
仅使用已存在的本地 Qwen3.6-35B-A3B-FP8；付费、O、兼容探针和新 G 均为 0。
两批额度已结束，未用额度不滚入后续批次，当前新增生成额度恢复为 0。

[P1/E0 工程准备](MILA_V0210_CONTROL_E0_20260908.md)保留其历史零生成结果。
P1 的 current_question/unresolved/next_action 现已由真实 Host 生成并逐字进入交付请求；
同时保留 C0 普通复核、C1 通用提纲，各两轮，模型/采样/完整材料/控制槽/输出上限一致。
工具对模型全部禁用，没有隐藏 Reader、线上 Reviewer 或来源投影；subagent 仅在批次后评分。
这是 **RESEARCH_PROTOTYPE**，不是原生 Codex 的默认能力验收，也不是模型自主 MCP 触发实验。

## E1：形式合法的 State 没有带来合格交付

首例仍是冻结的 `DERIVED_OPEN_DEVELOPMENT_CASE`，四份完整来源共 13,746 UTF-8 字节。
任务与基础历史经过预注册缩小，不能冒充 SIM12 同条件复现。一个案例不是三个独立 family。
评分按四个已冻结必要要求：特定操作结果、正常恢复动作、Evidence 提交前边界、保留与恢复原文。

| 条件 | PASS / 4 | 遗漏 | 矛盾 | 首次交付全部充分 | 准备＋交付 raw token |
| --- | ---: | ---: | ---: | --- | ---: |
| C0 普通复核 | 1 | 2 | 1 | 否 | 9,182 |
| C1 通用提纲 | 0 | 2 | 2 | 否 | 9,267 |
| C2 自产 Control State | 1 | 1 | 2 | 否 | 8,904 |

全部充分为 **0/3**；准备轮、首次交付中存在至少一项无依据断言/建议的产物分别为 **3/3、3/3**。
这是按可评价产物计的分母，不是所有命题的错误率，也没有把建议当作已执行操作。

C0 遗漏此次目录同步错误先于 TX-01 的必要结论，恢复建议混同 current-head 与特定操作回执。
C1 把请求超时直接说成连接获取阶段超时，并对已知提交前失败仍留下元数据是否引用的未知分支。
C2 的状态问题偏向具体故障原因和计时诊断；交付虽然引用了提交前错误传播，
却继续给出可能已提交的分支，并建议未写入元数据时清理孤立文件，违反保留原文要求。
内部 attention 未观测；这些是公开处理顺序、来源使用与建议的错误。

C2 相对 C0/C1 分别少 278/363 raw token（约 3.03%/3.92%），
但没有质量优势，不能宣称成本优化成功。正确 JSON 和引用到正确句子不等于正确使用来源。

## B2：能纠旧错误，但仍会引入新错误

两种受控场景各比较普通笔记 C0 与结构化控制记录 C2，每组只生成一次。
同场景来源/历史/当前观察完全一致，旧记录中的信息逐项匹配；这些是人工种子，
不是新的自然 G 或自然错误发生率。评分合同独立保存，不进入任一模型请求。

| 场景 | C0 | C2 | 结论 |
| --- | --- | --- | --- |
| 错误旧“已解决”＋明确新反证 | 撤销旧提交判断，反对清理文件；却把 fsync 错误套入 DatabaseCapacityError | 撤销旧判断，反对立即清理；却声称重试会产生两个相同 blob 副本，并转为等待重试/孤儿清理 | 纠正目标旧判断 2/2；产生额外无依据内容 2/2，完整语义安全不成立 |
| 当前信息足够 | 直接引用 ADR-047 回答关闭设置无需迁移 | 同样直接回答，无多余查询或保存要求 | 当前窄任务充分 2/2；C2 没有独立质量收益 |

错误旧判断被纠正不等于整个恢复说明正确；简单规则题通过也不证明复杂恢复能力。
无干预场景的 C2 使用 3,828 raw，C0 使用 3,796 raw；无需为此增加状态策略复杂度。

## 实际请求、费用和资源

| 批次 | 生成 | raw token | 模型请求耗时合计 | 批次墙钟 |
| --- | ---: | ---: | ---: | ---: |
| B1 / E1 | 6 | 27,353 | 28.365 秒 | 28.631 秒 |
| B2 / 语义负控 | 4 | 16,303 | 11.151 秒 | 11.347 秒 |
| 合计 | 10 | 43,656 | 39.516 秒 | 39.978 秒（两批相加） |

原始输入与输出都实计，准备轮不是免费。所有十次 finish_reason=stop，预约全部结清，
pending=0、bound violation=0、工具执行=0，没有重试、追问、输出修复或隐藏额外生成。
三次 E0 工程快照准备耗时合计约 0.895 秒，发现/tokenize 为其中约 0.556 秒；
实际运行的 discovery/tokenize 与批次墙钟另有记录，不把它们计成生成 usage。
文件存储字节见审计快照；GPU、电力和 subagent 审查/开发劳动成本未知，不填零。
E2/E3 未进入，所以没有本轮持久写入、恢复回查或维护成本，也没有“保存已回本”的证据。

独立审计从实际请求重建材料与控制槽，核对版本/span/正文、采样、逐字准备产物、
Provider 回执、完整输入 token、输出预约、请求与批次时间；它不使用关键词当语义评分。
语义评分由 `/root/b1_semantic_review` 完成，准备与首次交付分别保留。
脚本进程均已正常退出；没有启动产品服务、数据库、共享模型或部署进程，无需清理共享资源。
旧失败、旧投影原型及两条保留 Note 均未操作。

原始证据全部在 Git 外 `/cra/memory/mx_memory/evidence/v0210-control/`：

- `e0-final-r2-20260908/b1/`：六份实际请求/回执/可见产物、provider-ledger、result、subagent-review。
- `b2-20260908/`：四份实际请求/回执/可见产物、冻结配置/rubric、制品 pin、账本、subagent-review。
- `audit_batches.py` 与 `actual-batch-audit.json`：可重算的实际请求/费用审计。
- `completion-audit.json`：按原 Goal 要求和条件性阶段逐项核对的关闭依据。

## 原目标的阶段结论

| 要求 | 结论 |
| --- | --- |
| H1 / P1 / E1 | 无投影真实状态链路完成，三个自主条件和适用 B2 负控有结果；不支持本候选相对简单方案的质量/完整成本增益 |
| H2 / P2 / E2 | NOT_ENTERED：E1 没有足够积极且安全的进入信号；没有联想/暂停/换支效果主张 |
| H3 / P3 / E3 | NOT_ENTERED：未形成值得沿本策略继续持久保存的合格控制产物；没有真实冷恢复或跨会话净收益主张 |
| 机械负控 | scope/撤权/未知资格/超限/旧状态不滤新观察已由 P1 工程验证；不是新增 Product PG/RLS 验收 |
| 其余语义控制 | 候选暂存、暂停后恢复和不可用来源的模型行为属于未进入阶段，未标为通过 |
| 终态 | 满足 Goal §9 的否定诊断关闭分支；不是完整 E1→E2→E3 pilot 成功，也不是所有假设成立 |

后续若重新研究，需要新的通用机制理由、范围和阶段决定，不能用本次余额重跑同题。
研究愿景仍未证明；关闭的是本版有界候选，而不是把认知/跨会话研究问题改判为已解决。

## 工程交付

新增无投影 P1 方法、E0/B1/B2 入口、冻结配置/rubric与结果；保留旧投影历史。
Product 没有 API、Schema、权限、Canonical、事务、存储或默认策略改动，无迁移。
停止调用 Lab 入口即可撤下候选。真实 PG 和 Product 部署回归未重跑，因为没有对应语义变更。

本次新增 B2 的两项定向检查通过；最终 Lab 回归 **590 passed（7.10 秒）**，
boundary、Ruff、mypy（33 文件）、sdist/wheel build 均通过。
只排除 Goal 明令不继续运行的两个旧投影测试文件；此前 P1 的 27 项证据仍保留。

```bash
uv run pytest -q tests/unit/test_v0210_negative_controls.py
uv run pytest -q --ignore=tests/unit/test_state_focus.py --ignore=tests/unit/test_state_focus_preflight.py
uv run milai-lab-check-boundary
uv run ruff check src tests tools
uv run mypy src/milai_lab
uv build
uv run python /cra/memory/mx_memory/evidence/v0210-control/audit_batches.py
```

Schema 仍为 **0.1.x EXPERIMENTAL / Implementation CANDIDATE / NO-GO FOR SCHEMA FREEZE**。
