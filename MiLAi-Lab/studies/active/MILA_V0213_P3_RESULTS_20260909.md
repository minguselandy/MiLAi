# V02-13 P3：固定后两题完成，先解决任务合同缺口

终态：`COMPLETED_BOUNDED_DECOMPOSITION_KEEP_A0`。
用户要求“继续后续实验”后，execution revision 3 单独分配并运行原 accepted 前缀的剩余两题。
本批 **8 次本地生成 / 71,781 raw tokens**，全部结清；两批合计 **16 次 / 145,220 raw**。
累计 token 不设硬上限。Product 0.1.15/A0 保留，没有公网部署、真实业务动作或 State 新机制。

## 预生成发现：两题都无法从合法历史确定完整标准答案

这两题已在 P1 接纳，本批不修改问题、gold、原始 Session，不替换题目。
首次 P3 生成前，完整原文审查、原文区间、可裁定范围、评分合同 hash 和 A0/R1/O payload
均在独立 P3 Seal B 封存。旧 accepted、Seal A/B、首批结果及 executed-source 保持原样。

| 固定任务 | 原文支持和缺口 | 预注册处理 |
| --- | --- | --- |
| task-03 / qa_373 | 完整 33 turns 中同时给出三名学生及各自课程；“我第一门报名课程”的问题未把当前说话者绑定到其中一人。评价侧 evolution_chain 选中了一个学生，但它不是线上证据 | 身份有歧义；Oracle 不充分；全部条件不评完整参数正确/错误 |
| task-04 / qa_162 | 完整 55 turns 可识别东京行程、酒店名称、预订编号；没有酒店业务 ID。reference 把对话 source_id 的数字后缀当成酒店 ID，没有原文映射 | 缺少业务标识；Oracle 不充分；全部条件不评完整参数正确/错误 |

Oracle 原文候选分别保留 turns 22–28 的完整三人报名上下文、turns 0–15 的完整旅行对话，
没有通过裁剪隐藏竞争候选或补写缺失标识。两个 O 条件均 `ORACLE_INSUFFICIENT`，未发请求，
不是模型失败，也不是 token 不足。R1/A0 只观察取证、格式、弃答和未获支持的承诺。
没有自动语义判定器或额外 Judge；支持审查是编码代理的评价劳动，成本未单独计量。

## 实际观察

| 条件 | 尝试/响应 | 结构有效意图 | 完整参数可评分 | 结果 | 生成 / raw |
| --- | --- | --- | --- | --- | --- |
| A0 | 2/2 | 0 | 0 | 两题弃答 | 6 / 45,129 |
| R1 | 2/2 | 1 | 0 | FORMAT_ERROR 1；UNSCORED 1 | 2 / 26,652 |
| Oracle | 0/0 | 0 | 0 | ORACLE_INSUFFICIENT 2 | 0 / 0 |

task-03：A0 的三次生成依次选择 memory_search、working_state_get、弃答，两个工具返回均已呈现。
它没有读取原始历史。R1 收到完整 Session，指出三名学生存在但当前用户身份不明，要求补充信息；
这段文字合理表达了不确定性，但实际编码是 `action=business, calls=[]`，故记格式失败，
不能记为答错学生参数。上一批后的空调用终态修复在此实际生效；没有补答或重试。

task-04：A0 同样查空记忆和工作状态后弃答，未读取旅行历史。R1 全文可恢复酒店名称、评分、
距离、价格和预订信息，却提交 `hotel_id=0.0, locale=en_US, domain=Tokyo`。
`0.0` 没有来源支持，`en_US` 与问题的日文要求冲突；这是可见的未获支持承诺和直接要求偏离。
但完整标准答案预先已判不可裁定，故整体记 `UNSCORED`，不事后改成参数错误计入分母，
也不把无效 Oracle 包装成“充分证据下的 use failure”。结构有效不等于语义正确。

两题 A0 都重复了首批的一种路径：source-only 历史可读，却只访问未摄取历史的空 Product。
这不证明 Product 检索丢失已摄取历史，也不证明改用 source 接口就能答出缺失的身份/业务 ID。
零生成回放原 memory_search 字符串到同一 source_search，两题均未命中；没有改 query 或扩大召回。
因此，本批不能重复首批 task-01“原查询即可命中”的特定观察。

A0–R1、A0–O、R1–O 的本批完整参数成对分母全部为零。
R1 均触发 `FULL_HISTORY_FITS`，未测试 BM25 的实际排名效果。
结合[首批报告](MILA_V0213_RESULTS_20260909.md)，四个 accepted cluster 都已处理：
10 个条件实际执行、2 个 Oracle 因证据不足保留未执行记录；不再有未尝试的 accepted 任务。
只有首批两题可裁定，R1/O 各 2/2 正确；不能把本批塞入首批 accuracy 分母。
没有同题修复前后对照，不能宣称交付判定修复改善了模型生成质量。

## 最小工程变化与后续方向

Lab 增加可续跑批次选择：只能接续原 accepted 顺序，拒绝重复、跳题、未接纳题；
再次 prepare 在写任何输入前拒绝已有 Seal B。P3 使用独立目录和 execution-policy，旧首批封存不覆盖。
收尾现在区分结构有效意图与语义可评分意图，并正确记录已处理前缀、P3 和缺陷诊断终态。
新增 7 个回归用例覆盖前缀续跑、封存保护和不可裁定意图的零参数分母。
不修改模型可见提示、schema、seed、模型、R1 策略或 Product 实现。

下一候选是 **任务合同与缺失输入处理**：在评价合同中明确当前主体绑定、业务 ID 的合法来源，
区分业务 ID、预订 ID 和对话 provenance；确实缺失时定义澄清/弃答的交付合同。
这不是对现有 gold 的事后修复，也没有实现自动检测器。新行为比较需另行封存新合同，
不消耗确认集、不补换本批题，不自动启动 State、S1/S2、绑定机制或维护策略。

## 验证、成本与边界

`uv run pytest -q`：**676 passed**；`uv run ruff check src tests tools`、
`uv run milai-lab-check-boundary`、`uv run mypy src/milai_lab`（38 文件）、`uv build` 全部通过。
Product pin verifier 在创建隔离实例时检查固定交付包和已安装 Python 文件；四个冷 Host
分别核对实际八工具 catalog、模型身份及 65,536 context。每请求输出预约 4,096，最多三次 A0/
一次 Reader，1,200 秒整批上限，实际 41.507 秒。原 raw cap 为 null，实际不发生 token 停止。
R1 两次全文装配约 0.0141/0.0127 秒。预备/审查/网络等未完整单独计量，不虚报免费。

8 个请求的 payload/tokenize/HTTP usage/Provider 账本逐一核对，无 pending 或 violation；
四个实际工具返回全部呈现，无记忆写入。Oracle 两条件零请求，不计两次独立回答。
独立 API PID 4098317 已消失；owned compose `v0212-3f9c0d72df35` 的 PostgreSQL Exited(0)，
卷与原证据保留。共享模型没有重启；Product API/schema/权限/Canonical 无改动、无迁移。
Schema 仍 `0.1.x EXPERIMENTAL / Implementation CANDIDATE / NO-GO FOR SCHEMA FREEZE`。
98 个 Mem2Act confirmation 分组、旧 Horizon 55 分组/712 题及其 16 道未跑题保持封存/原状态。

Git 外证据目录：`/cra/memory/mx_memory/evidence/v0213/p3-admission-20260909/` 与 `p3-20260909/`。
P3 Seal B SHA256：`724c365a941797a096cfe268d07aaf1643b8ae72a310ab9aaa96f2c37ee79bd2`。
P3 terminal SHA256：`a6a0df6c1ff2bed6ebac19028a70d922a201e8cc51fc513ed1a1eccff0a22527`。
旧首批 Seal B/terminal SHA256 复核不变；原请求、结果和分母没有重算覆盖。
