# SIM12：有界原始来源回查与剩余解释错误

状态：`ACTUAL_FULL_SOURCE_PRESENTED_TASK_CONDITIONS_NOT_PASSED`。
这是开放开发验证，正式D4/D5尚未进入，完整Goal继续IN_PROGRESS。

## 实现与边界

现有Lab Host增加可选`host_file_backcheck=ALL_IF_WITHIN_BOUND`，默认OFF、G不执行。
从可信来源清单读取原始文件，在8文件/32768编码字节的本次边界内完整呈现；超出文件数、
原始大小或实际JSON编码大小时不预载部分正文。保留首尾、空白和完整UTF-8，不按题目、
答案或业务字段挑选内容。L2不是原始来源，不混入该回查。大材料仍可用现有分页/搜索工具。
文件读取同时校验路径、哈希和文件大小，防止预检后文件增长造成无界读取。

先核验公开来源资格，再获取正文，呈现前复核；每次模型发送前重新检查已呈现来源，
撤权不能靠历史消息重发绕过。运行结束后另修正一个依赖记账问题：最后一份文件复核失败时，
整个未呈现bundle不应留下前几份文件的context_refs。现在先完成全部复核，再一次登记引用；
保留既有上下文依赖。该修正晚于SIM12，不将运行时Host源码pin改写成修正后的版本。

无Product、公开API、数据库、Canonical或治理变更；未新增Reader、Hint或Reviewer。
这是一种可选Host装配策略，完整来源预载有成本，不设为日常默认，也不据此宣布低token记忆。

## 实际链与输入证据

逐字节复用SIM08的真实G文件；新G请求0，原G的34,480 raw只在历史计一次。
与SIM11使用同一续做题、六项来源判据、通用提示、原生思考、8192输出上限与既定本地模型。
新增因素为两组共同的有界原始来源回查；A仍预载L1/L2，B只预载L1，两组完整文件与工具相同。
独立项目经公开导入/保存，operation确认及当前head分别核对，再由两个新进程冷恢复。

Product pin：`97eaac6de36a96e903af0b41412a44e8c7715539ca12359b7be41f790a5754bc`。
实际Host工具和wrapper摘要见运行根`execution-source-pin.json`，运行配置不覆盖。

两组的`HOST_FILE_BACKCHECK_RESULT`均含ADR-042、ADR-044、ADR-047及计时合同四份完整原文，
共13,746原始字节。终态审计核对实际Provider请求、全文连续呈现、哈希、公开引用及六项
冻结来源片段。一次query回查仍未完整返回条件5/6，但额外文件回查已补齐；两个观察不能混算。
B请求未出现完整L2正文；所有G文件保持一致，三个State身份不同。两组均首次请求即交付，
自主查源工具调用0。没有将内部reasoning文本当作证据或进行评分。

## 首次交付的开发者核对

| 固定必要条件 | A | B |
|---|---|---|
| 1 操作结果与更高head分开 | 保留UNKNOWN；把写入超时扩大成确定未获得连接，并提出无来源的缓存/幽灵读取可能性，不能全项通过 | 保留UNKNOWN，未据高版本确认本次成功；没有充分解释更高head与具体操作的区别 |
| 2 不盲目覆盖/自动重写 | 明确禁止盲覆盖和自动重试，未实际写入 | 明确禁止盲覆盖，未实际写入 |
| 3 目录同步失败先于Evidence/outbox提交 | 遗漏本次未提交的必要结论；文件持久性未知不填补此缺口 | 明确说无法确定TX-01是否已提交，和冻结来源的提交前错误边界矛盾，FAIL |
| 4 保留来源与既有重放合同 | 提到验证/同步和显式重放；未充分说明操作关联条件，不能默认PASS | 提到验证、同步再提交；泛化套用未知事务使边界仍不完整 |
| 5 每池/进程tenant限额、关闭不迁移 | 正确说明每池/进程、非全局配额、非通用公平、关闭不迁移 | 覆盖同样必要内容，不再与普通pool等待队列配置混淆 |
| 6 缺失计时与嵌套汇总 | 明确缺失不填0，嵌套不与父时段相加 | 同样覆盖 |

没有新增Judge；以上仅为固定来源与首次交付的开发者比较，整体文风、计划质量及不可见推理
未评价。遗漏、歧义与明确相反断言分开记录，两组均不满足整个任务合同。
条件5随完整原文呈现得到纠正，但一次跨批变化不构成普遍因果效果。条件3已有完整原文仍失败，
其剩余归属是解释/交付；继续扩大来源或重复同题不能作为已解决它的证据。

## 成本、权限与收尾

| 组 | 请求 | input | output（含原生思考） | raw | 在线秒 |
|---|---:|---:|---:|---:|---:|
| A | 1 | 11,313 | 5,297 | 16,610 | 39.054 |
| B | 1 | 9,786 | 4,550 | 14,336 | 33.878 |
| 合计 | 2 | 21,099 | 9,847 | 30,946 | 分会话报告 |

准备/公开保存18.234秒，模型之后权限检查1.594秒，整批94.054秒；在线时间涵盖绑定、恢复、
回查、工具、生成与交付。B少2,274 raw，但质量不合格，不能宣布分层净收益。
相对SIM11，新增来源使两组总raw增加9,206；输入和输出均实际计入，不将输出变化单独归因。
缓存/思考细分未取得则保留未知；付费0，不将本地GPU、电力和开发劳动算作免费。

模型结束后，独占环境通过真实公开MCP撤销A的一份来源：之前A/B都能完整回查，之后A不返回
任何bundle正文，B仍完整。另有Host集成回归证明呈现前撤权阻止首次发送、后续撤权阻止重发。
此公开检查0新模型请求，不等于真人OAuth撤销验收。

两次Provider回执与预约结清，pending/violations均0。Host/API/worker进程已退出，PG退出0、
无OOM且卷保留，共享服务未改。tracked配置已关闭新发送，运行根原始配置保留。
累计SIM01–12为69请求、1,304,756 raw；20完成会话、4失败会话、1个零生成失败分配，
共25终态分配。完成不等于任务正确。

本轮工程验证：46项定向测试通过；全Lab440通过（4.12秒），boundary、Ruff、mypy 30文件、
build通过。负控覆盖完整首尾、超限、哈希变化、符号链接、读取中增长、资格未知、撤权及
未呈现bundle的依赖记账。Schema仍0.1.x EXPERIMENTAL / NO-GO FOR SCHEMA FREEZE。

证据：`artifacts/v02-e2e-generality/sim12-planning-source-backcheck-20260907a/`中的
`execution-result.json`、`terminal-audit.json`、A/B请求与回执、`host-file-backcheck.json`、
`post-model-revocation-check/`。审计入口为同级`audit-sim12-planning-source-backcheck-20260907.py`。
完整D3服务出口、正式D4和D5留出/变体仍未完成；下一步回到尚缺的服务工程出口，
不以继续重跑已打开规划题替代完整Goal。
