# MILA-V02-05：8并发Runtime CPU诊断

状态：`API_CPU_DEMAND_CONFIRMED / EXCLUSIVE_GIL_CAUSE_NOT_ESTABLISHED`。
本轮没有修改产品源码、配置、公共接口或资源上限；用保留采样及11个有界Product内部请求
确认读取计算量与并发耗时放大的关系，不产生新的公开MCP SLO结果。

## 保留采样

读取f的`resource-samples.json`与`recall-events.json`。使用本机runner继承的monotonic时间
确定阶段，只取完全位于阶段内部的首尾样本，以进程utime+stime增量计算平均CPU核数。
采样约0.5秒，不能据此定位请求内切换或将Runtime/MCP逐条强配。

| 阶段 | 内部采样区间秒 | API平均CPU核数 | worker平均CPU核数 |
| --- | ---: | ---: | ---: |
| c1-r0 | 5.060 | 0.656 | 0.006 |
| c1-r1 | 5.015 | 0.654 | 0.002 |
| c1-r2 | 5.047 | 0.658 | 0.004 |
| c8-r0 | 3.995 | 1.019 | 0.008 |

API实际affinity为0–1；源码公开CLI使用单进程Waitress和8线程。这里没有PG进程CPU采样，
不把API接近一核解释为整机或PG空闲，也不把这组数据单独作为GIL独占根因证明。

## Product内部受控请求

脚本和输出位于Product `runtime/var/v02-c8-cpu/`。只启动f已经停止的自有PG，复用原1000
来源；原API/worker/MCP未重启。Flask test client进入真实`/v1/memory/resolve`路由，原
reader凭证、Host binding、scope、50结果/120候选/8192上下文token/2000ms预算全部保留。
进程及8线程继承CPU affinity 0–1，连接池保持原8；没有修改共享服务或调用生成/tokenize。

预先固定11请求：1预热、1单请求、屏障同时启动的8请求、1单请求CPU profile。
非profile测量使用`time.thread_time`，整批另计`time.process_time`与墙钟时间。环境为
CPython3.11.13，线程CPU时钟为`CLOCK_THREAD_CPUTIME_ID`。这是Product开发诊断，省略
真实HTTP/MCP/Waitress入口，不是Lab黑盒效果或公开SLO校准。

- 单请求：墙钟181.522ms，线程CPU141.114ms。
- 同时8请求：每请求线程CPU157.724–165.362ms，墙钟1041.822–1307.852ms；合计请求
  线程CPU1290.801ms，整批墙钟1334.481ms，进程CPU1425.711ms。进程CPU包含辅助线程，
  与请求线程之和分别报告，不能相加。
- 所有11请求均HTTP200、结果PARTIAL；除预热外的10个完整Memory Context与预热逐字段
  相同，无删除字段或ID规范化。PARTIAL保持原含义，不解释为语义完整或模型任务正确。

单请求CPU profile采用线程CPU计时，墙钟397.575ms、线程CPU356.672ms，明显高于非profile
141.114ms；分析器开销很大，函数排名只用于候选定位，不把profile百分比当正常请求成本。
候选包括Evidence解释/日期扫描、来源投影、requirement绑定、deepcopy与JSON序列化。
其中deepcopy有9367次递归调用、61个外层调用，profile对高调用数Python函数的扰动尤其大，
不能直接把显示的76ms当作正常复制耗时。

## 下一步边界

请求墙钟放大而线程CPU仍约160ms，整批消耗接近一核；与公开采样共同支持“API计算需求
是当前瓶颈之一”。没有证明GIL、PG调度、连接等待中某一个解释所有长尾，亦未运行禁用
GIL/多进程的因果对照。不通过提高并发、线程、连接或默认跨请求缓存来凑300ms门槛。

源码审查已定位普通LOOKUP的`STATE_OBSERVATION`需求仍经完整解释路径；既有type-directed
路径虽然能剪除候选，但为记录suppressed计数仍扫描日期/事件，直接开关并不能证明省下
主要计算。`RetrievalExecution.context_candidate_items`另外为60个候选做深复制，必须先
确认下游修改隔离和消费者合同，不能仅因profile排名而改成浅复制。

下一份工程工作先检查这些既有路径和重复计算的真实消费者，以固定输入证明必要输出、
来源/权限、诊断与生命周期合同后再做最小修复；不增加新Reader或测例专用分支。

本轮Product源码pin保持
`0fe5f7cf08ee3ea2a6d5813dee56aa3726508314cc4fce3cc06fd4aa0e345027`；前置pin验证通过。
没有源码/公共配置变动，未重复上一轮1022/309全量回归，亦不将它们登记为本轮新测试。
诊断脚本已实际执行11请求；退出时两数据库客户端关闭，自有PG再次停止并经inspect确认，
原服务未重启，数据保留。Schema仍`NO-GO FOR SCHEMA FREEZE`，无迁移或部署。
新增模型/Provider tokenize均0；完整P1/P2/P4及D4/D5未完成。
