# SIM10–SIM11：任务明确度、来源依据要求与输出容量

状态：`SIM10_OUTPUT_TRUNCATED / SIM11_DELIVERED_TASK_CONDITIONS_NOT_PASSED`。
两批都是开放开发，不是正式D4/D5或留出验证。原SIM08/09判据与结果保持。
任务修订的依据、范围及运行前合同见[MILA_V02_GROUNDED_TASK_CONTRACT_20260907.md](MILA_V02_GROUNDED_TASK_CONTRACT_20260907.md)。

## 运行条件

现有`completion_guidance`已支持通用来源依据要求，因此没有新增Product或Host代码模块。
新版任务明确询问计时缺失/嵌套汇总和可选配置范围；原六项来源判据原样保留。
SIM10同时改变任务表述与通用提示，不可将跨批差异归因于单条提示的独立效果。

两批逐字节复用SIM08的全部真实G文件，G不重做、不追加摘要；公开导入四份来源到三个
隔离项目，保存L1/L2引用与operation回执，再由新进程恢复。只重绑定公开声明的来源ID；
适配器Evidence集合的排序差异有记录，业务payload与数组保持。
每批仅A/B各一新会话；A预载同一L1/L2，B只预载L1；普通文件、来源、工具、思考模式、
Host回查及通用提示相同。任务不是评价器在模型运行中追加，评价文件不进入Host工作区。

Product pin：`97eaac6de36a96e903af0b41412a44e8c7715539ca12359b7be41f790a5754bc`。
每会话100万raw/600秒/64请求，批次200万raw，只有本地既定模型、无付费或Judge。

## SIM10：单次输出容量不足

| 组 | 请求 | input | output | raw合计 | 在线秒 |
|---|---:|---:|---:|---:|---:|
| A | 1 | 7,933 | 4,096 | 12,029 | 30.941 |
| B | 1 | 6,420 | 4,096 | 10,516 | 30.614 |
| 合计 | 2 | 14,353 | 8,192 | 22,545 | 分会话报告 |

两组均返回`finish_reason=length`，A仅有509字符最终content片段，B没有最终content；
原生reasoning存在但不评价其内容。未尝试修补/执行截断JSON，没有工具动作或最终交付。
已核对实际请求、输入计数、Provider回执和总usage，UNKNOWN与预算违规为0。
本次是输出完整性失败，不计作语义答错，也不能评估通用提示的任务质量效果。
保存/冷恢复与实际来源呈现仍有直接证据。准备及保存18.064秒；全批80.565秒。

## SIM11：明确扩大输出上限后完成交付

遵循用户完成优先指令，在下一独立批次只把单请求输出上限由4096改为8192。
原题目、六项判据、提示、G文件、思考模式、工具及总预算保持。预算观察消息自然反映新上限；
未静默扩额、重试未知请求或覆盖SIM10失败。G仍复用而不重新生成。

| 组 | 请求 | input | output | raw合计 | 在线秒 |
|---|---:|---:|---:|---:|---:|
| A | 1 | 7,927 | 3,138 | 11,065 | 24.866 |
| B | 1 | 6,425 | 4,250 | 10,675 | 31.365 |
| 合计 | 2 | 14,352 | 7,388 | 21,740 | 分会话报告 |

两组返回完整`stop`内容，无超额、超时或未知预约。准备/保存21.394秒，全批78.844秒。
输出上限变化同时会改变实际预算提示，跨批也存在运行身份等正常差异；不以单次结果声称
8192上限必然降低生成量或时延。B本批少390 raw，但质量尚未合格，不能据此采用分层默认。

## 首次交付与六项必要条件

开发者直接依据冻结来源核对首次最终交付；没有另开Judge、没有改写原模型答复。
两组依然首次请求即finish，主动来源读取0。回查完整呈现条件1–4的冻结片段；
ADR-047没有进入回查，计时文档只返回部分内容。原文件与后续检索都仍可用。

| 必要条件 | A | B |
|---|---|---|
| 1 不用更高head推断超时操作结果 | 明确保留UNKNOWN；没有据高版本宣布该操作成功 | 列出本次或其他提交的可能性，保留操作结果未知 |
| 2 不盲覆盖/自动重写 | 禁止盲覆盖，但“重放或读取当前版本以对齐”未厘清操作确认；相关语义不默认PASS | 明确禁止将超时当失败回执盲覆盖；未实际重试 |
| 3 识别目录同步失败先于Evidence/outbox提交 | 未完整满足：仍仅讨论文件持久性未知，没有确认本次提交被阻止 | 未完整满足：说明提交前需同步，但没有明确本次Evidence/outbox没有提交 |
| 4 完整来源、同步边界及既有重放合同 | 未完整满足：引用显式重放，但来源保留与操作关联条件未充分说明 | 引用现有恢复契约且无实际操作；来源保留/幂等条件仍不完整 |
| 5 tenant限额的每池/进程范围 | 未通过：把明确的可选tenant限额拆成两种猜测，没有补查ADR-047并给出准确作用域 | FAIL：将tenant上限明确写成`database_pool_max_waiting`及其默认32等待队列，属于不同配置 |
| 6 缺失计时及嵌套口径 | 覆盖：缺失不填0、内部时段不与父时段相加、错误汇总不支持性能发布结论 | 覆盖同样必要内容；将未记录保持为未知 |

遗漏不一律作为相反事实断言，含糊项不硬判正确。两组仍不满足完整任务合同；
B有明确的配置混淆错误，A选择保留配置歧义而没有利用可用来源完成任务。
新版题目明确要求计时判断后，两组实际给出了正确相关内容，但这不单独证明提示或记忆改善。
整体文风、计划完备性和内部推理保持NOT_EVALUATED。

## 归因与下一步

已有材料的误解和缺少材料是两个问题：条件3的相关完整来源已呈现，不能归因于来源缺失；
ADR-047未呈现且两组未继续查，是获取覆盖与Host/Agent继续查证选择的问题。
仅增加通用提示、原生思考和输出容量没有确保完成任务，停止继续堆提示或无差别扩额。
下一项最小机制应验证有界原始来源回查是否能取得被一次检索遗漏的文档，并独立观察
取得后是否真正理解；不把回查全部成功当作语义正确，也不增加隐藏Reader/Reviewer。

## 账本、验证与清理

两批新增4请求、44,285 raw tokens；历史G只计一次。两批含准备及清理共159.409秒，
开发劳动及两批之间的诊断间隔另列。付费0，reasoning包含在completion总数中，
缓存及reasoning细分未返回，不用缺失值充当0或推算金额。

SIM01–SIM11累计67请求、1,273,810 raw；18个执行完成模型会话、4个失败会话、
1个首次生成前失败分配，共23个终态分配。完成状态不等于语义通过。
当前两批自有Host、API、worker退出，PG均退出0无OOM；保留所有卷、请求、回执和失败。
共享服务保持，批次新发送关闭。没有Product/Host源码、API、权限、Schema或Canonical修改。
配置/评价hash、相同六项判据、实际Host提示装配及所有实际请求均已验证。
交付检查：Lab422 passed（3.86秒），boundary、Ruff、mypy（30文件）及build通过。
终态审计单独证明保存、恢复、完整文件保留、实际输入和用量；语义判定以本表为界。

证据：`artifacts/v02-e2e-generality/sim10-planning-grounded-20260907a/`及
`sim11-planning-output-capacity-20260907a/`中的`execution-result.json`、`terminal-audit.json`、
`generator-reuse.json`、A/B原始请求/回执及最终交付（SIM10无最终交付）。
配置：`configs/v02-project-planning-grounded-20260907.json`、
`configs/v02-project-planning-output-capacity-20260907.json`。
完整Goal保持IN_PROGRESS，D3整体与正式D4/D5未完成；Schema仍NO-GO FOR SCHEMA FREEZE。
