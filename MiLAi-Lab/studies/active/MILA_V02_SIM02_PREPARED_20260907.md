# MILA-V02-05：SIM02已准备，等待独立诊断执行决定

后续更新：用户已明确“授权运行”，本批次已执行并关闭，见
[实际执行结果](MILA_V02_SIM02_EXECUTED_20260907.md)。以下为执行前的历史准备记录。

状态：`PUBLIC_SOURCE_BATCH_PREPARED / MODEL_AUTHORIZATION_ZERO / FORMAL_D4_NOT_ENTERED`。
本页是具体可审阅的下一批次，不产生模型额度或工程阶段PASS。

## 已完成的准备

运行根：`artifacts/v02-e2e-generality/sim02-ready-20260907a/`。
沿用既定已打开开发材料0a995998，没有挑题、删除首尾或削弱来源。
G/A/B各导入484条完整来源并逐条公开GET核验正文、原始时间、角色、链路及项目资格；
三臂共1452条公开回执，来源ID互不重叠，各有完整44份会话文件。三个初始State均为ABSENT，
没有清空自然保存的State。实际G尚未启动，因此也没有产生候选L1/L2或续做结果。

来源/评价文件按原字节封存，评价引用和问题时间经过现有启动校验；G任务不含具体续做
问题或评价答案。源码在准备前记录摘要，准备后核验未变。Product pin为
`56a0dab6df6dedd4540d3cc5a59fc1f9013505275e91cb7050b3d8eb56482e0c`。

为保持既定资源，prepare增加可选compose override并记录其摘要及CPU亲和性；本批次采用
`tools/containers/v02-local-bounded.compose.yaml`：PG 2CPU/1GiB，进程CPU0/1。
启动前核验运行专属服务名无既有容器/卷。准备耗时35.843896秒，初始化/来源准备与会话
30秒预算分开记账，artifact约6.70MB。准备后自有API、worker、三个MCP均退出，PG停止，
数据/凭据保留在私有运行目录；执行前需恢复同一专属服务并重新检查资格，不重新导入来源。

本地`GET /v1/models`返回200，公布配置中的`Qwen3.6-35B-A3B-FP8`；这只证明当时模型ID可用，
不证明usage合同或任务效果。本轮生成/tokenize均0。调用实际run入口被
`NO_NEW_MODEL_AUTHORIZATION_HISTORICAL_BATCH_CLOSED`阻止，尚无分配文件或Provider账本。

Lab 372项及boundary/Ruff/mypy/build通过；Product源码未改。运行器准备参数的相邻20项检查
通过，真实Docker配置复查为2CPU/1GiB。没有以资源放宽、输入删减或改题获得准备成功。

## 提请决定的独立本地诊断批次

拟议使用当前已准备数据，运行G/A/B各最多1会话；每会话累计raw input+output最多20,000，
批次最多60,000，每会话30秒、最多10请求，单请求输出最多1200。预算覆盖重复历史、恢复、
工具和G必要保存。预约先落盘、生成单在途、未知usage保留预约并停止；不重试、不换模型、
不追加G或扩样。G没有合格产物或有效比较机会则保留该结果，不能补摘要制造保存成功。

比较为同一G完成点的A预载L1/L2、B仅预载L1并按需展开；完整文件、合法来源及普通工具一致。
两臂公共来源重绑定和后续State隔离继续使用现有公开接口。结论限定于这个本地Host、模型、
开发材料和预算；记录任务首次实际使用边界、最终产物、实际输入及成本，无可靠语义判据的
部分保留未评价。单链不提供留出泛化或正式D4通过结论。

## 尚未满足的正式门

| 项目 | 当前证据及限制 |
|---|---|
| 当前范围检索延迟 | 约532.5ms已被用户接受，不再阻塞后续工作 |
| 多租户公平/组合故障 | 仍未完整验证；同tenant的两个项目不能替代多租户证据 |
| 混合负载退化 | 历史有超过1.5比例的轮次，未改写为通过 |
| Host/Provider成本 | 本地预约、deadline、请求归属已有分项验证；原生/付费Provider及完整金额合同未验证 |
| 正式D4/D5 | 工程门与相应额度尚未齐备，均未进入 |

若用户决定让该独立本地诊断先于余下服务验收执行，需要同时明确新增3会话/60,000 token额度。
批准只适用于此诊断，不豁免后续正式工程门，不授权D5或付费模型。
当前配置的transport仍false，新增额度仍0；准备完成本身不是执行许可。
