# SIM06：选择性记忆的来源覆盖提示

状态：`COVERAGE_GUIDANCE_INSUFFICIENT_BOTH_FIXED_ASSERTIONS_FAILED`。
这是已打开开发材料上的机制诊断，不是D4/D5或留出泛化确认。原SIM04/SIM05错误答案保留。

SIM05中两组都有普通完整文件搜索，但均只核验了笔记已有的一件物品。本次只增加一段
通用Host完成提示：选择性笔记可能遗漏历史，结论依赖覆盖时应检查遗漏；来源访问方式
由Agent决定，不要求读完所有文件，不把没有查到等同于不存在。不含答案词、来源位置
或额外模型。提示以`completion_guidance`配置保存，在所有实际模型请求中核对到。

配置`configs/v02-local-sim06-coverage.json`，产物
`artifacts/v02-e2e-generality/sim06-coverage-20260907a/`。与SIM05的授权配置比较仅
`execution_id`和`completion_guidance`不同。Product pin仍为
`cdccd06328cd07eb77c2fc3956c3df742c30b2c38ae5aa7ee65a7d3c31c30c5c`。

原G产物字节不变，没有新G模型请求；重新独立导入与公开验证来源身份，再公开保存，
新A/B进程分别预载详细记忆与State优先。完整文件、题目、来源断言、模型、工具及宽预算
一致。来源UUID及运行环境时序不是字节级相同，跨批不作确定性因果或统计显著性声明。

| 组 | 请求 | input | output | raw | 在线秒 | 冻结必要条件 |
|---|---:|---:|---:|---:|---:|---|
| A | 2 | 55,196 | 126 | 55,322 | 21.916423 | FAIL：1≠2 |
| B | 2 | 50,534 | 114 | 50,648 | 20.663686 | FAIL：1≠2 |

两组实际都只调用`read_file(session-011.json)`，搜索调用0；随后给出1件外套，并额外
断言没有其他需要取回/退回的物品。固定来源要求2件，漏掉同一双换货boots。首次实际交付
边界判FAIL，不能把这段最终答复改标草稿，也不能把显式更全面的措辞当作覆盖成功。
该结果反驳“在当前条件只追加通用提醒就足以触发遗漏核验”的本例假设，不证明任何
Host/模型下提醒都无效。其他未经可靠评价的语义维度仍NOT_EVALUATED。

独立于Agent的事后只读诊断，用题目原词`pick up`、`return`调用普通文件搜索：74命中、
5页，第一页第15/16条已含session-019的boots相关片段；session-030还可通过后续分页找到。
该搜索遍历完整冻结文件，未改排序或加答案词。它证明当前工具和原始材料存在可达入口；
**不是模型选择了搜索，也不是这些片段曾进入实际模型上下文**，没有再次检查线上权限。
完整诊断在`offline-search-diagnosis.json`，实际行为在每组tool-events及请求正文。

因此本次失败可定位于选择性记忆后的来源覆盖/行动选择，不能把“工具可检索”当成
“Agent实际查证”。不据此增加Hint planner/reviewer，不继续在本题上逐次改提示直到答对。
后续若研究Host边界的简单来源回查，应单独声明激活差异、成本及新任务范围。

新增4次生成、105,970 raw tokens；准备36.675419秒，全批82.110907秒。G成本不重复记，
付费和Judge均0，pending/违规0。两个Agent/API/worker已退出，PG停止无OOM，配置关闭。
截至SIM06累计45次本地生成、889,599 raw tokens；9个模型会话执行完成、2个模型会话
执行失败，另1个分配在生成前失败。执行完成数不是任务正确数。

Lab全套389通过（3.52秒），boundary/Ruff/mypy/build通过。Goal仍IN_PROGRESS，
服务公平性/完整SLO、正式开发链及冻结后的跨家族/表示变体仍需证据。
