# V0218 E2：同信息控制完成，未选出独立机制候选

状态：E2_COMPLETE / SELECTED_MECHANISM_CANDIDATES=0。
原 [E2 设计](MILA_V0218_E2_CONTROL_PLAN_20260911.md) 完整执行，没有按中间结果换题、
换 A、改提示词或追加重复；全部结果为 EXPLORATORY，不是独立确认。

## 已执行条件与有效性

Run：`/cra/memory/mx_memory/evidence/v0218/20260911/e2-controls-v1`。
日程与新闻两个原 lineage、两个 coarse family，均为已暴露 D；不是 30 个独立任务。
每个 root 一个新自然 A；2 A 均实际业务 PASS 且自主写 Note，NO_WRITE=0。
30 B 分别重新创建进程和公开记忆 scope，原样复制该 A 实际 Note，继承同一业务/历史。
A 不加消费策略或预读；C1/C2 的 E1 提示逐字不变。

五臂 N1/C1/C2/R1/P1 × Stable/Superseded/Unresolved 全部运行。
B 首次生成前各真实读取 current/policy/records/history，未裁剪的响应、版本/hash
在每次真实 HTTP 请求中逐字审计；同 root/world 五臂呈现完全相同。
120 次读取标 HARNESS_PUBLIC_PREFETCH，不记作模型自主取证。30 B 全部旧 Note/当前来源双呈现。
之后普通 read 仍可用；没有工具内模型复核、私有 checker、未来事件或理想 Note。

这是新的 MATCHED_PUBLIC_INFORMATION 条件：本次自然 A 与 E1 不同，新闻 A 本次完整通过，
E1 两次新闻 A 则只完成两项事实。因此不能把 E1 与 E2 分数变化归因于预读，亦不合并效应。
原默认 Host 路径不启用预读；本次共同新分支另封存，不能声称沿用完全相同 Host 文件 hash。

实际 32/32 FINISHED，130 requests / 448148 raw，unknown/pending/violations=0。
没有 episode 用尽 16 次生成，没有 4096 输出截断或 deadline 终止；最后一次仍预留 finish。
批次 351.68 秒，API 已停止、compose stop=0、卷 RETAINED。
公开 Runtime/MCP/Client 仍分别为 0.1.4/0.1.15/0.1.3 的独立已验证发布件。
工作树 product.lock 漂移未修改/未拿来替代已验证 pin。

## 完整配对结果

P/F 是完整环境 checker 通过/失败，不能用正确 Note 或 finish 声明替代。

| 原 root / world | N1 | C1 | C2 | R1 普通复核 | P1 近邻概念 |
| --- | --- | --- | --- | --- | --- |
| 日程 Stable | P | P | P | P | P |
| 日程更新 | F | F | P | P | F |
| 日程未决 | F | F | F | F | F |
| 新闻 Stable | P | P | P | P | P |
| 新闻更新 | F | F | F | F | F |
| 新闻未决 | F | F | F | F | F |

完整 B：C2/R1 各 3P3F，N1/C1/P1 各 2P4F。两原 root 的样本不足以支持普遍性或独立机制因果。
所有 Stable 都保持合法状态；P1 在新闻未决另有两次精确重复业务写入。

## 行为和实际 Note 的非盲复核

复核者为执行开发者，已知 arm；无独立人审、无 Judge。以实际源/动作/版本和完整状态作依据。

- 日程 A：两场原合法预约，C04 为 03-26 13:00–13:30/301/A。
  原 Note 事实正确，digest `44ace0152846ec8dae05d24c2461a49f7ff1e204d3bd2831238c7d07c232d7af`。
- 日程更新：N1/C1 已收到全部新事实，仍对充分确定的模式/时段提出不必要澄清并保留旧预约。
  C2 在 turn 0 保存检查点，实际更改两条业务记录，turn 4 达到完整合法状态，6 calls。
  R1 同样先保存普通复核，在参数拒绝后实际修正两项，turn 6 完整通过，8 calls。
  R1 自发组织了旧/新差异，还误把两天的相同时刻当作潜在冲突；最终时段仍合法。
  因而不能把“没有结构指令”冒充“生成内容绝无结构”，也不能用其错误中间推断取消真实成功。
  P1 看到了变化并实际写入，但把 30 分钟面试写成整个 09:00–11:00 窗口，失败为 INVALID_DURATION。
- 日程未决：五臂都真实请求了 C04 澄清，却都没有撤去其旧 confirmed=true 预约。
  C2/R1 另正确更新 C03 模式；其余三臂还留下 C03 旧模式。部分修正不算完整 PASS。
  request_clarification 明示不会删除既有记录；该失败不是没有提出问题，也不是机械次数耗尽。
- 新闻 A：四项事实均实际写完，Note 事实/来源正确，digest
  `acac59394771a19623bc6a9ff487ec3738feb7f5c33446c1e05f222b0e6ee8c6`。
  E2 新闻更新的 N1/C1 直接 finish；C2/R1 保存声称工作已合法的 Note 后 finish，均没有业务更新。
  C2 的新 Note 形成虚假的“已按当前来源复核完成”声明，说明写检查点本身不能保证执行。
  P1 看到了 ed2/discharged 并实际更新，但引用混入旧 edition，完整溯源仍失败。
- 新闻未决：N1/C1/C2/R1 没有实际澄清或纠正旧已确认事实。
  P1 正确写入两项 null/conflicting 及冲突来源，却只在说明中提到待核验，未实际提交澄清，仍 FAIL。

上述是条件明确的动作差异。没有随机干预具体 Note 内容/组件实现，不能据此把每个错误归因为 Memory。
此矩阵不是针对共同错误起点的恢复试验；all-cause 纠正机会只作描述，不报记忆诱发 Recovery 因果率。

## 机会与成本

共享 A 只计一次：22 calls / 51850 raw。下表为每臂 6 个 B，不含共享 A。

| 臂 | calls | raw tokens | Note 写次数 | 保存文本字符总数 | Host 秒数 |
| --- | ---: | ---: | ---: | ---: | ---: |
| N1 | 15 | 44485 | 1 | 243 | 23.28 |
| C1 | 18 | 54951 | 0 | 0 | 25.20 |
| C2 | 22 | 77331 | 6 | 5493 | 40.73 |
| R1 | 26 | 105383 | 7 | 11985 | 60.64 |
| P1 | 27 | 114148 | 6 | 10365 | 56.80 |

四次公开预读/B 的字节在重复模型输入内计费；HTTP tokenization 预检并保留每个实际请求。
匹配的是输入资料与可用机会，不是实际字数/GPU 工作量相同。
C2 比 R1 少 4 calls / 28052 raw 是本批工程效率观察，不证明结构的独立新颖性；
功能优先下，不能因 N1 更便宜就声称其质量充分。R1 比 C2 长的复核也未取得额外完整成功。

## E2 消融闭环与候选卡

同信息由全字节预读控制；同机会由共同 16 次上限/输出预约控制；C1 为去检查点指令；
N1 为自由 Note 基线（E0 另已比较一句提醒 N2）；R1 为普通复核；P1 为近邻概念。
所有臂同冷边界/同源 A，同有 Stable、更新、未决。没有缺失对照用“未来再做”代替本轮筛选。

### C1 v1：不晋级

观察：E0 计划/事实与实际业务脱节；最小操作为 B 消费时提示逐项核对证据和已执行工作。
反证预测：若仅提示 audit 不足，充分资料下仍可能停在旧工作；E1 两次日程更新均失败，
E2 同信息日程和新闻更新也失败。强对照 N1 与其完整质量相同；E2 C1 0 次 Note 写入。
成本：E1 两批 B 合计 70 calls / 194581 raw；E2 18 / 54951。
原始 runs：`e1-discovery-v1`、`e1-discovery-r2-v1`、`e2-controls-v1`，均保留失败。
范围仅两个已暴露 root/一个模型；风险包括不必要暂停及虚假完成。没有独立代码状态机制，
近邻普通复核可覆盖其操作；生成随机性和自然 A 差异未排除。结论：NOT_QUALIFIED。

### C2 v1：工程信号保留，机制卡不晋级

观察：E1 日程更新两次相对 N1/C1 的真实成功；最小操作为 C1 后、首个业务动作前
要求一次自由证据/行动检查点。冻结反证：R1 能达到同样行为则无需 C2 特定结构解释。
E2 C2/R1 完整结果逐对相同，C1 去组件质量差异只在一个日程更新；
未决没有完整修复，新闻检查点还写入错误完成声明。特定结构必要性未获支持。
成本：E1 两批 B 合计 68 calls / 180764 raw；E2 22 / 77331。
原始 runs 同上，C2 提示未调整、第二次不利的新闻效率结果未删除。
作用范围仅 B 消费，不改变自然 A 的写作/维护，不能声称端到端 adaptive offloading 收益。
安全风险是用可信外观 Note 掩盖未执行业务；非盲自审和两个 root 限制仍在。
直接近邻见 [prior-art 复核](MILA_V0218_PRIOR_ART_20260911.md)：本轮 P1 是公开披露的概念适配，
非官方复现；其失败不证明 C2 超越完整 StateMem。R1 自发组织结构是残余混杂，
但已足以阻止“专门结构指令不可替代”的主张。结论：ENGINEERING_SIGNAL_ONLY / NOT_QUALIFIED。

合格机制卡 0，E3 NOT_TRIGGERED；E4 NOT_TRIGGERED，且 C=0 的独立确认缺口保留。
这不是 BASELINE_SUFFICIENT：已证实有未解决失败，关闭的是本批机制候选，不是全部研究方向。

## 封存索引与复核

同一 run 目录下：

| 文件 | SHA256 |
| --- | --- |
| manifest.json | f08eace928ac83e3a3e76e0484f5594edc1ce39e4a9b1f7ca88fff2432777dcd |
| result.json | b1242e740baa3ef67c8c32a4ca4eee5de75ba271830623d558ac2fbcd0979f74 |
| control-audit-v1.json | 4c478b986c69a6406042649bfa721210352f488a99dafdc9c4108c945b6d6dcf |
| control-summary-v1.json | 3445b902dd9d15a9f764987d07edcfbdb291c8810acafd57a2bb9c96f08a87f9 |

`tools/audit_v0218_e2.py` 离线复核完整 HTTP/冷 Note/四类预读/全部业务快照；
`tools/summarize_v0218_e2.py` 只产生描述统计，不替开发者作语义裁定或选择。
1258 tests / 1 optional SDK skip、boundary/Ruff/mypy/build/diff-check 通过。
全 Goal 总账为 1340 requests / 3353114 raw；最终验收与限制见 E5 决策。
