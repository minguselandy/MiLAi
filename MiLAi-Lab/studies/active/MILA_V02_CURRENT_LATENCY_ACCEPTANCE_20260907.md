# MILA-V02-05：当前公开延迟复测与用户接受

状态：`CURRENT_OBSERVED_LATENCY_ACCEPTED_BY_USER / FULL_E2E_INCOMPLETE`。
用户在看到本次结果后明确表示“525ms可以接受”，随后补充“500多ms可以接受”。
因此接受当前约532.5ms的完整客户端P95，不再将降到300ms作为继续闭环工作的前置条件。
这是用户修改当前可接受性能范围；历史300ms检查的失败结果保留，不改写为原门槛通过。
本次不据此发明一个新的精确上限，也不追加性能确认运行。

## 当前版本公开结果

运行：`artifacts/v02-e2e-generality/p1-scoped-recall-20260907l/`。
配置：[v02-scoped-recall-lazy-offsets.json](../../configs/v02-scoped-recall-lazy-offsets.json)。
Product pin `56a0dab6df6dedd4540d3cc5a59fc1f9013505275e91cb7050b3d8eb56482e0c`，
运行前后均验证；本轮Product和Lab运行源码未改。
相对旧k配置只改运行身份、pin及变更说明，保留1000条完整来源、512/2048/8192字节载荷、
首尾、两个项目各500条、权限、工具、资源和调用检查。PG 2CPU/1GiB，Runtime CPU0/1，
pool8、线程8、并发上限8；20批公开就绪及3个范围控制通过。

| 阶段 | 样本数 | 完整客户端P95 ms | 收到响应P95 ms |
|---|---:|---:|---:|
| c1-r0 | 24 | 120.205776 | 112.543516 |
| c1-r1 | 24 | 94.536318 | 88.223429 |
| c1-r2 | 24 | 108.660310 | 100.409594 |
| c8-r0 | 24 | 532.501852 | 524.218766 |

原运行器按冻结300ms规则在c8首轮后停止，终态为`STOPPED_AT_CLIENT_P95_BOUNDARY`。
未执行其余c8轮次或扩大并发。现在该已观察延迟得到用户接受，停止以该差距驱动微优化。

SDK物理调用1021，MCP工具调用99，Runtime记录1125；这些是不同边界，不能相加。
返回97次有界Context的DEGRADED、1次MISS和1次预期拒绝，未把DEGRADED记作HIT。
MCP结果未提供可用请求ID映射；98次Runtime resolve计时只作总体诊断，不能强行关联
c8阶段、相减各层P95或将数据库持有时间全归为SQL计算。网关排队与租户公平性仍未验证。
没有新模型、Provider tokenize或付费依赖调用。

## 清理与下一步

自有API1029755、worker1029756、MCP1029877均已退出；PG容器已停止且非OOM，数据保留。
共享vLLM/公开MCP未改。清理回执和进程存在性复查保存于运行目录。
首次分析命令用了缺少httpx2的系统Python；改用现有uv环境完成分析，没有重跑负载。

当前工作转向剩余恢复、隔离、失败处理与Host成本边界。接受此延迟不等于已验证租户公平、
混合负载退化或模型效果；正式D4/D5和新增模型额度仍按原合同。保存/恢复、任务质量、
分层增量、迁移及总成本继续分别评价。完整Goal仍进行中，Schema仍为NO-GO。
