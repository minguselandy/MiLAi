# V02-12：前瞻准入与有界 A0 诊断完成，保留基线

终态：**A0_DIAGNOSTIC_COMPLETE_KEEP_BASELINE**。完成的是 Horizon discovery 的首波与
一波条件性复现，不是全量 benchmark、完整历史直输或 State 机制验收。
实际 **4 个 cluster / 8 题，10 次本地生成，69,325 raw tokens**；6 题交付单字母答案，
官方规则与 local strict 均为 **2 对、4 错**，另 2 题预算停止、无最终答案。
评分覆盖 6/8；已评分正确率 2/6，不能把未交付题删除后宣称整体成功。

首波仅确认 1 个 cluster 的有限普通偏好使用错误；下一固定前缀没有新增独立同类失败，
依预注册规则停止。确证的关系/适用性机制失败为 0，候选门未达到，不提出 S1/S2。
V02-11 仍是 BLOCKED/INCOMPLETE，其队列、保护分区、结果和预算均未重开。

## 题池与两次封存

用户明确要求执行 V02-12、人工审查用 subagent 代替，随后明确允许自行下载项目/数据。
这些指令覆盖原文档的规划暂停。第一波单列 12 次生成/80,000 raw 上限；出现符合 §4 的
宽义 presented-but-wrong 后，第二波另立同额分配，沿用原 Seal B 前缀及相同 A0 Host。
第二波没有继承第一波余额；其后没有第三波、干预或 confirmation 分配。

STALE 固定 commit `ea7d391103a151927cd29d2f01d87597a782bdcb` 的官方树与 releases
未提供可定位的论文固定场景包，仅有生成框架及 seeds，因此主池未进入。
没有生成新数据或执行其自动 Judge；此结论限于核验过的发布位置。
见[官方 STALE 目录](https://github.com/icedreamc/STALE/tree/ea7d391103a151927cd29d2f01d87597a782bdcb/STALE)。

Horizon 代码固定为 `4b5076b147c952499b7921f20f811dd5aca7ef0b`（Apache-2.0），
数据 revision 为 `50941f00f90c03a5a60219d76393869b757b835a`（数据卡 CC-BY-4.0）。
六个本地 Parquet 文件共 1,601,956,117 bytes，全部匹配该 revision 的 LFS SHA256。
只解码允许的中性 metadata 后得到 4,245 题、346 个完整 user_id，其中 evolved 2,484 题；
不以 README 的笼统规模代替实际 inventory。数据来源与归属见
[官方数据卡](https://huggingface.co/datasets/stellalisy/HorizonBench/blob/50941f00f90c03a5a60219d76393869b757b835a/README.md)。
FactConsolidation 仅核验发布元数据，作为未分配 fallback；未打开其 QA 或构建第三套后端。

用户 ID 与规范化同名关联产生 254 个保守 metadata cluster。规则为完整 user_id 内合并，
对 item ID 的姓名执行 NFKC/casefold/空白规范化，并对同名的全部 user_id 合并。
跨生成器裸 user_N 只是本地编号，不据此合并。关联组不证明真实同一人物或不存在隐藏同源。
subagent 检查旧冻结索引及本地历史中性 ID：120 个旧 Formal 题、10 个旧 smoke 题，
传播至关联 cluster 后排除 89 组；额外发现的历史记录均回指这 130 题。
有限声明是“与冻结本地索引未发现重叠”，不是模型预训练未见过的保证。

| 层级 | Cluster | 原池关联问题数 |
| --- | ---: | ---: |
| 原池 | 254 | 4,245 |
| 旧保护/暴露传播后的并集排除 | 89 | 2,247 |
| 允许候选 universe | 165 | 1,998 |
| Discovery | 110 | 1,286 |
| Confirmation，正文保持封存 | 55 | 712 |

Seal A 在 discovery 正文/评价字段审查前固定 universe、来源关系、排除快照、seed、排序、
每组代表用户与 evolved/static 各一题、12 组目标、60 组筛查上限、资源和评分/Host profile。
采用 Goal 指定的紧凑 JSON/UTF-8/SHA256 算法；先排除旧保护/暴露关联组，再按 cluster 做 2/3 划分。

顺序筛查 13 组，接纳 12 组/24 道预选题；1 组因预选用户缺少题型配对而拒绝，97 组未筛查。
这次拒绝在 ACCEPT 前沿冻结队列推进，未替换任何运行失败。
Seal B 在首个生成前固定接纳清单、逐题官方参考与评分合同、完整来源及 hash。
24 题的资格/评分审查记 REVIEW_OPENED；其余 confirmation 正文及 gold 均未打开。
实际只执行接纳清单前 4 组/8 题，余 8 组/16 题明确 NOT_RUN。

## 实际行为与归因

| Query | 题型 | 最终答案/官方规则 | 最早可证实的归因 |
| --- | --- | --- | --- |
| q001-1 | evolved | E，错 | PRESENTED_SUPPORT_WRONG：当前页面已含编号步骤及分析逻辑偏好；仅支持普通偏好使用错误 |
| q001-2 | static | A，错 | UNKNOWN：未确证能区分选项的必要偏好支持 |
| q002-1 | evolved | D，错 | SOURCE_NOT_ACQUIRED：较早相关语境未呈现；该语境是否足以唯一判别仍不确定 |
| q002-2 | static | 无最终答案 | RESOURCE_LIMIT：第三次完整预约超过单题 20,000 raw |
| q003-1 | evolved | D，对 | 官方规则正确；不自动证明完整获取或理解 |
| q003-2 | static | E，对 | 官方规则正确；不自动证明完整获取或理解 |
| q004-1 | evolved | A，错 | UNKNOWN：已呈现页面缺少区分致歉选项的必要偏好支持 |
| q004-2 | static | 无最终答案 | RESOURCE_LIMIT：第三次完整预约超过单题 20,000 raw |

所有已交付答案均为明确单个字母，无多选歧义；4 个错误均不是旧偏好 distractor 选项。
官方规则、严格格式及语义归因分别保留；UNKNOWN 没有覆盖官方错误标签，也没有成为机制信号。
关系/适用性错误在已裁定 support-presented-and-wrong 中为 **0/1**。
这个很小的条件分母不能解释成普通工作流发生率或 State 无效。

语义审查为用户授权的 **MODEL_ASSISTED_ADJUDICATION / SUBAGENT_DERIVED**，
不是官方 supporting-evidence 标签或独立人类 gold。原始 source byte offsets、hash、实际
请求内片段及判断限制在两波 `semantic-adjudication.json` 中；没有猜测内部注意力。

完整历史写入每题独立的普通只读源文件，共 24 文件、15,154,879 bytes，未总结、截断原件
或以 gold 筛句。Host 初始只看到明确标注为 partial 的原文末尾 ≤4,096 bytes，全部原件可经
冻结的文件读取/字面搜索访问。八个真实 compact MCP 工具也提供给 Host。
因此本 profile 是 **NONSTANDARD_LOCAL_DIAGNOSTIC**，不冒称官方 full-history 协议。

8 个新 Host 进程各用独立 principal/project/task。6 个交付答案均未发工具调用；另外两题
共调用 3 次 `milai_memory_search`（MISS）及 1 次 `milai_working_state_get`（ABSENT）。
它们没有调用 `source_read`/`source_search`，也没有把普通文件摄取进 Product Note/Evidence。
两题最后一次工具结果只写入工具账本，第三次请求停发，因此这些结果未呈现给模型。
不能把空 Product store 的返回当完整历史无支持，也不能据本次普通文件能力声称 MiLAi 净收益。

## 成本、失败改进与收尾

| 波次 | 本地生成 | Raw tokens | 墙钟 |
| --- | ---: | ---: | ---: |
| P2 首波，2 组/4 题 | 5 | 34,454 | 28.40 s |
| P3 条件性复现，2 组/4 题 | 5 | 34,871 | 26.85 s |
| 同池同 profile 合计 | 10 | 69,325 | 分波记录 |

每个实际请求输入 ≤8,192，输出完整预约 1,024；每题最多 3 次生成/20,000 raw，
每波 12 次/80,000 raw。账本预约全部结清，没有未知 usage、隐式重试或超限发送。
benchmark Judge、付费服务、数据生成、模型权重下载和 AGENT_MEMORY_MUTATION 均为 0。
模型 discovery/tokenize 是零生成请求，单列于 preflight/实际输入证据；它们不等于无资源成本。

P0/P1 从资源分配到 Seal B 为 830.55 s，筛查与源导出为 110.21 s。
最初文件名搜索遗漏了已有 Horizon 目录，导致重复下载；发现本地数据后停止该下载，
保留已落盘的 506,462,208 bytes partial 证据，验证并复用既有完整副本。
下载器现已先检查本地大小和固定 LFS hash；修复实测复用全部六文件，新增下载0 bytes。
这是下载落盘字节，非包含协议开销的完整网络计量。官方目录/数据卡网络账本、
独立 PyArrow 21.0.0 工具依赖下载、审查与 CPU/磁盘劳动另记；未完整计量部分不写成零成本。
历史索引字段路径的一次解析错误在 Seal A 前修复，没有打开 QA 或消耗生成。

两个预算停止暴露了本 Host 的交付空间问题：重复携带工具定义及已有输入后，
虽然还有第三次请求数，剩余 raw 已不足以完整预约下一次输出。原停止结果完整保留。
后续若研究改进，应另冻普通来源选择与交付预算方案，先比较源文件路径和工具目录开销；
当前材料不支持为此增加 State schema、自动维护或结构绑定。

固定 MCP 0.1.15 / client 0.1.3 / Runtime 0.1.4 制品与已安装 Python 文件逐字节核对。
两波独占 PostgreSQL/API/MCP 均已停止，新卷保留；旧服务、共享 vLLM 与旧数据未重启或清理。
Product 代码、schema、权限及 Canonical 实现无改动，保持 **0.1.x EXPERIMENTAL /
Implementation CANDIDATE / NO-GO FOR SCHEMA FREEZE**。

新增 8 项必要机械测试通过；完整适用 Lab suite **609 passed**，仍排除已废弃的两套 projection
原型测试。boundary、Ruff、mypy（37 个源文件）、build 及实际 artifact/预算复算通过。
运行后 recurrence 控制脚本仅做格式整理，已保留原执行源码并核对 AST 相同；cold Host 未改变。

原始证据：`/cra/memory/mx_memory/evidence/v0212-external/`。
关键产物为 `p0-20260909/{seal-a,seal-b,exposure-manifest,cluster-manifest,screening-ledger}.json`、
`evaluation/`、`online/`、两波请求/工具/Provider 账本、评分与语义审查、`terminal-result.json`
及 `completion-audit.json`。本轮按无新增独立复现停止，保留 A0；未打开确认集，不追加第三波。
