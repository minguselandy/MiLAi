# V02-13：历史取得与使用分解完成，修复动作交付判定

终态：**COMPLETED_BOUNDED_DECOMPOSITION_PROPOSE_HOST_FIX**。
完成零生成旧轨迹回放、Mem2ActBench 顺序准入、首波两题三条件运行及一个 Lab 交付判定修复。
保留 Product 0.1.15/A0、八工具和现有记忆架构；没有公网 MCP 交付或真实业务动作执行。

用户明确要求执行原规划，并随后要求“开放 token 消耗，功能实现优先”。
执行 revision 2 因而取消新实验累计 raw 上限，输入受实际模型上下文限制，输出完整预约 4,096；
保留首波两题、最多十次生成、单调用 60 秒、单工具 40 秒、整波 1,200 秒。
原规划文档的零授权/不执行描述是编写时状态，历史 P0 仍按原 20k/1,024 复算。
批准与暴露审查由本次 subagent 完成；没有新增 benchmark Judge、数据生成或付费模型调用。

## P0：十次历史请求全部对账，两次停发请求完整重建

复用 V02-12 已执行八题，逐条核对 payload SHA256、完整 tokenize、Provider usage、可见输出和工具账本。
历史总计仍为 **10 次 / 69,325 raw**，本次回放新增生成为零。
有效 tokenizer 来自现有本地服务环境的只读导出，固定 backend JSON、chat template 和版本；
本地直接加载模型目录曾少算一 token，原因是 Transformers 版本的预分词规则不同。
导出有效配置后，全部十次离线计数与实际历史 usage 精确相等。

| 停发题 | 已消费 raw | 完整第三次输入 | 原 20k 下 headroom | 仅 JSON 空白压缩后的机械 headroom |
| --- | ---: | ---: | ---: | ---: |
| q002-2 | 14,452 | 8,149 | -3,625 | 83 |
| q004-2 | 13,610 | 7,580 | -2,214 | 1,483 |

第三次 payload 含实际第二轮输出、真实工具结果、最终交付提示及完整 response schema。
先前已发送的第二次 payload 也由第一轮轨迹重建，并逐字节核对，验证同一重建方法。
未发送输入/预约输出不计为实际消费；两题各有一个旧工具结果未呈现。
按完整渲染 prompt 的 tokenizer offsets 将 token 唯一分配到内容类别与首次/重复发送，
跨类别 token 单列 boundary residual，字节和 token 总和分别核对。
response_format 属解码/传输约束，单列 wire bytes，不重复加到实际 prompt usage。

唯一 P0 compact 候选仅去除 JSON 结构空白，保留所有解析字段、字符串、原件、来源和工具能力。
该结果仅为 `H0_COMPACT / MECHANICAL_HEADROOM_ESTIMATE`，未运行行为比较、未部署；
按用户指令，token 优化不作为当前功能工作的前置条件。

## P1：固定制品、完整来源与前瞻分区

固定 [Mem2ActBench 仓库](https://github.com/Cantaloupe-M/Mem2ActBench/tree/b00726940b5abbe9bd324bdd7a2cb272f5c62a29)
commit `b00726940b5abbe9bd324bdd7a2cb272f5c62a29`，实际制品为
`Mem2ActBench/qa_dataset.jsonl`（400 题）和 `toolmem_conversation.jsonl`（2,029 Session）。
下载共 34,051,602 bytes；核对固定 Git blob ID、大小和 SHA256，再次准备复用原件，新增下载零。
顶层 README 声明 MIT，数据 README 说明完整 Session 的评价用法；没有独立 LICENSE 文件。
仅作本地研究，不分发数据，不把该声明扩大为已证明所有上游数据权利。

规则在任务正文审查前冻结；先由程序抽取 ID/lineage 和字段结构，再按 source、完整 Session、
QA 演化来源构造连通 cluster。22 题来源映射无法确定，隔离；305 个有映射的任务分组中，
12 组因旧 BFCL 暴露/保护传播排除，剩余 293 组按冻结 hash 顺序分为 discovery 195、confirmation 98。
四个 discovery 前缀任务接纳，首波固定前两个；后两个仍 `ACCEPTED_NOT_RUN`，未替换运行失败。

subagent 从旧中性索引确认 108 个重叠 BFCL 原件：8 个 dev 暴露、100 个 test 保护。
保护集用旧 universe/dev 中性 ID 重建并与原 test hash 对齐，未打开保护正文。
ToolACE/oss 的发布内 source→Session 映射可核对；旧约定索引中未发现其来源暴露记录。
上游数字 ID 的跨版本全局对应、完整历史暴露和模型预训练未见过均未获证明，不能声称全新总体。
这里的独立性限于固定发布制品内不共享来源的两个 cluster 及已核对的本地暴露索引。

所有完整原始 Session 行逐字节写入普通源文件；实际 turns 完整保留，不依赖发布中较小的 turn_count。
线上只传问题、原作者提供的工具 schema、原文及中性来源入口。
gold、grounding_info、evolution_chain 与评分说明仅保留在 evaluator 目录，读取接口仅允许冻结源文件。
Python `dict/int` 等类型名只在评分 schema 校验时统一转换，线上原始业务工具定义不变。

首波输入、完整来源 hash、R1 配置、两份 Oracle 原文包/充分性审查和逐题评分在首次生成前 Seal B 封存。
R1 的固定政策为全文可放下则全文，否则 1,024-token 块/128 overlap、BM25 1.2/0.75、top-4；
共同证据容量按用户要求放宽至 49,152 tokens，实际模型上下文为 65,536，输出预约 4,096。
这是 execution revision 2 的条件，不是原规划的 8,192/1,024 限制。两题均触发 FULL_HISTORY_FITS，
因此本次效果结果不验证 BM25 排名性能；排名分支有机械测试。
Oracle 第一题含原始 turns 4–11，第二题含 29–45，保留第二题后续其他城市天气语境。
原始业务历史工具调用是来源数据，允许保留。R1/O 完整 payload 均不相同，未共用响应。

## P2：两个不同瓶颈，不能合并成一个语义失败族

全部运行使用 `SOURCE_FILES_ONLY`、真实本地八工具 MCP 和逐题/逐组隔离作用域。
Product 没有摄取这些历史，MISS 仅描述空记忆分支。
首波 **8 次生成 / 73,439 raw**，全部 usage 结清；累计 token 上限停止为零。

| 条件 | 尝试/响应 | 有效业务意图 | 参数可评分/正确 | 其他结果 | 生成/raw |
| --- | --- | --- | --- | --- | --- |
| A0 | 2/2 | 0 | 0/0 | 拒答 1；格式失败 1 | 4 / 30,226 |
| R1 | 2/2 | 2 | 2/2 | 无 | 2 / 25,352 |
| Oracle | 2/2 | 2 | 2/2 | 无 | 2 / 17,861 |

task-01（qa_044）：A0 最后拒答。它花费 22,914 raw，第三次请求已实际发送，
但仅调用 memory_search 与 working_state_get，未读取存有坐标的普通源文件。
两个工具结果均已呈现；这是 `SOURCE_NOT_ACQUIRED`，不是预算失败或错误参数答案。
R1 全文和有效 Oracle 都返回正确坐标。零生成回放将 A0 原有查询字符串送入同一合法
source_search，首个命中再经普通 source_read 即返回完整坐标；没有看 gold 改 query 或扩大命中数。
这些回放结果未呈现给原模型，只证明既有源入口可到达支持，不证明它当时必定答对。

task-02（qa_175）：A0 初始页含 Sydney 五日预报摘要；它正确说出 Sydney 并列出五日天气，
却返回 `action=business, calls=[]`，没有提交调用意图。原 Host 写为 ANSWERED 的事实保留，
按预注册完整调用评分记 `FORMAT_ERROR`，不把缺失参数意图写成参数推理错。
R1/Oracle 均返回 `GetWeatherForecast(location="Sydney", days=5)`。
days 的作者 schema 默认字段与描述不一致，但本任务明确回指五日请求，不采用任一默认值。

A0–R1、A0–O 的有效参数成对分母都是零；R1–O 为二，均正确。
可以比较两题的工作流交付状态，不能据此报告 A0 参数 accuracy 0/2 或统计确认主因。
两题均为 `KNOWN_TOOL_PARAMETER_GROUNDING / ACTION_INTENT_ONLY`，不计工具选择或真实天气调用成功。
未出现跨任务重复的关系/适用性 use failure，State/S1/S2/绑定/自动维护均不启动。

## P4：已实现最小交付判定修复，未追加行为实验

`business_delivery_status` 现在要求非空且结构完整的 business call plan 才返回 ANSWERED。
空调用的文字回复记录 FORMAT_ERROR；不追加隐藏 Reader、补答、重试或工具调用。
这是 `HOST_DELIVERY_CANDIDATE` 的机械判定修复，不声称模型因此会生成正确调用。
回归测试覆盖实测空调用形状；原运行源码已按 Seal B hash 保存到 Git 外 executed-source，
旧请求、输出、Host 状态及分母均未覆盖。P0 的空白压缩候选未启用。
P3 无新分配且两个问题类型不同，停止在首波，不使用剩余调用次数补题。

## 验证、成本和收尾

最终检查：`uv run ruff check src tests tools`、`uv run pytest -q`（**669 passed**）、
`uv run milai-lab-check-boundary`、`uv run mypy src/milai_lab`（38 文件）、`uv build` 全部通过。
新增 16 个必要回归测试覆盖计数不重叠、缺失重建证据、原文保真、R1 gold 不变性、Oracle 标签隔离、
cluster 排除/确认分区、接纳后不替换、完整参数/空意图判定，以及放宽累计额度后仍结清 usage。
实际 MCP catalog 与冻结八工具定义逐项相等，运行前核对固定交付包及已安装 Python 文件。
subagent 最终审查指出并已修复两处收尾/准入分支：未运行、无效 Oracle 和输入别名现在从聚合账本
保留状态与零生成分母，不要求不存在的 arm 文件或伪造第二份响应；未知来源隔离现在传播到完整关联簇。
原 Seal A 曾仅隔离 22 个无法映射 QA，其 confirmation 中有一个未接纳的两题簇仍与已知部分来源关联。
已追加整簇 quarantine（`lineage-quarantine-supplement.json`），原顺序/hash/98 个封存分组保持不变，
没有打开该簇正文；四个 accepted 和首波都不接触它。不能把原 Seal A 描述为已完全隔离所有未知关联。

P0 tokenizer 诊断有 1 次 `/tokenize` HTTP 请求，离线回放和导出没有生成；其诊断时长未单独计量。
R1 两次全文获取/装配各约 0.01 秒。P1 元数据、Oracle 和 subagent 审查劳动不视为免费能力；
单独货币成本、CPU/磁盘总劳动及协议层网络成本未完整计量，不填成零。
新增真实生成与所有重发输入、输出均在 73,439 raw 内；原历史 69,325 不重记为新消费。

六个冷 Host/MCP 与自有 API/PostgreSQL 已停止；API PID 消失，owned compose PostgreSQL 显示 Exited(0)，
卷与原始证据保留。共享模型及旧服务未重启，未开放公网，没有业务动作执行。
Product 代码、API、schema、权限与 Canonical 均无改动，无迁移；Lab 修复可单独回退。
Schema 保持 **0.1.x EXPERIMENTAL / Implementation CANDIDATE / NO-GO FOR SCHEMA FREEZE**。
V02-12 仍 KEEP_BASELINE，V02-11 仍 BLOCKED/INCOMPLETE；旧 55 个 Horizon 确认分组/712 题及
16 道未运行题保持原状态，本次 98 个 Mem2Act 确认分组正文也未打开。

代码：`src/milai_lab/methods/acquisition_use.py` 与 `tools/*v0213*.py`。
原始证据：`/cra/memory/mx_memory/evidence/v0213/` 下 P0 cost-replay/measurement-lock、
P1 release/exposure/Seal A/B/online/evaluation/host-inputs、P2 请求/工具/Provider 账本、
`evaluation-outcome-review.json`、`terminal-result.json` 和 executed-source。
这些结论是有界本地诊断，不是官方 Mem2ActBench accuracy、Product retrieval 净收益或改进后模型行为验收。
