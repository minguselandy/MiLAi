# V02-11 external diagnostic: qualification stop

Execution status: `STOPPED_RESOURCE_OR_PROTOCOL_LIMIT`.
X0: `PARTIAL_NOT_X0_READY`. **A0 external QA diagnosis has not run or completed.**
There are zero benchmark generations, zero benchmark raw tokens and zero judged answers.
No accuracy, retrieval failure rate or State-use conclusion can be computed.

The user's execute instruction superseded the document-only planning pause. Development,
metadata access and user-authorized subagent review proceeded within the first-stage
contract. The existing protected partitions and the fixed no-substitution sequence were
retained; no new State mechanism, Product change, paid Judge or old-budget reuse occurred.

## The gate that stopped the first wave

The metadata exposure review found no new V1 population: all 500 question IDs are covered
by 300 historically consumed items and 200 old held-out items. Changing to the Oracle
file does not change question identity. V2's old text-only partition contains 42 opened
development questions and 380 protected questions. The other 29 question-image items
are outside that partition, but all are gotchas; ID novelty alone did not certify that
their underlying tasks or sources were independent of tuning.

The metadata-only SHA256 queue fixed ten gotcha positions, alternating web and enterprise,
before opening candidate questions or answers. It left all unavailable stratum positions
explicitly empty. No gotcha surplus was transferred to the other capabilities.
Subagent review then compared the ten candidates with only the 42 already-opened V2
questions and specific source trajectories, leaving the protected 380 questions unopened.

| Qualification | Count | Meaning |
| --- | ---: | --- |
| EXPOSED_NEAR_DUPLICATE | 4 | Specific underlying task/relationship overlaps with old development |
| UNKNOWN | 5 | Task/source independence cannot be certified from available evidence |
| CERTIFIED_DISTINCT, limited scope | 1 | Reviewer-derived distinction relative to the reviewed 42 old questions; not a pretraining or universal novelty guarantee |

The first-wave gotcha slot, `81180ae9`, overlaps old address-editing questions `289aea4a`
and `2ca2d443` and the actual address-editing trajectory mapped to `source-098`. This is
substantive task overlap, not a claim that all questions sharing irrelevant haystack
filler are duplicates. The annotation is `SUBAGENT_DERIVED`, not an official support label.
The one provisionally distinct question, `77258cda`, occurs later in the frozen queue.
Moving it into the first wave would violate the no-substitution rule. No generation ran.

| Original target positions | Terminal accounting |
| --- | --- |
| V1 24 | No eligible new partition; NOT_RUN |
| V2 dynamic/workflow/premise/static 35 | No eligible candidates outside the old partitions; NOT_RUN |
| V2 first gotcha 1 | Exposed near-duplicate; NOT_RUN |
| V2 subsequent gotchas 9 | Sequential branch not entered; NOT_RUN |
| Total 69 | 59 unfilled positions + 10 named candidates; **not 69 sampled or evaluated questions** |

The first wave's nine planned positions contain eight unavailable positions and one
exposed candidate. There is no valid first-wave use-failure result with which to enter
an expansion branch. All 60 later positions retain their reasons. The conditional
relation-failure numerator and support-presented-wrong denominator are **0/0, N/A**;
absence of observations is not a negative finding about the model or State.

## Work delivered

The Lab now has metadata-only inventory and hash pinning, an exposure exclusion manifest,
fixed candidate positions and branch rules, ten offline per-question evaluation contracts,
a query-blind complete Small source adapter, whole-source/image integrity checks, readiness
gates and terminal position/branch accounting. Evaluation contracts preserve official
answers and task-specific requirements, counterexamples, abstention and dispute rules.
Official supporting trajectory labels are `NOT_PROVIDED`; derived source references are
separate. Unresolved source causality is not promoted into a use-failure verdict.

The full two-domain Small histories contain **200 trajectories, 5,095 states and 5,095
screenshots**. Every ordinary goal, outcome, initial URL, state index, step, URL, action,
public trajectory thought and accessibility-tree field matches its original value.
Only opaque trajectory/image identifiers and mechanical wrapping change. All source
states remain available; no answer-directed pruning, summarization, captioning or OCR
was used. Original image hashes, PNG checksums and compressed pixel streams verify.

Export produced 173,611,681 text bytes and 545,600,649 image bytes in 207.56 seconds.
It reused local archives of 3,354,163,660 and 2,562,302,847 bytes, and required no new
trajectory, screenshot or model-weight download. This is HARNESS_INGEST into normal
source files, not Product Note/Evidence ingest. AGENT_MEMORY_MUTATION is zero because
no experimental Host ran. Shared-source amortization has no executed-question denominator.
Metadata networking and review labor were not fully metered and are not silently free.

Product MCP 0.1.15/client 0.1.3/Runtime 0.1.4 delivery bytes were verified, including the
21/14/252 installed Python files. A subsequent zero-generation X0 check started a new
owned PostgreSQL/API instance and exercised the actual eight-tool MCP catalog. All eight
names and input schemas match the pinned public contract. Eighteen public MCP calls
created two synthetic Notes and two synthetic States in separate principal/project/task
scopes. Fresh MCP processes recovered each scope's own content; cross-scope Note-ID reads
were denied in both directions. These four writes are **SYNTHETIC_BACKEND_PREFLIGHT**,
separate from the zero benchmark AGENT_MEMORY_MUTATION count. The check took 16.46 seconds;
its API/MCP/PostgreSQL services are stopped and its new volume retained. No old instance
was restarted. The worker and embedding prewarm were disabled; the configured embedding
provider is deterministic_hash, with no external embedding model request.

The ordinary source interface exposes only a domain's frozen text/image paths, literal
search and exact paginated reads. The first lexicographic file in each domain reconstructed
its original hash across 24 and 7 pages; both literal searches returned 16 permitted-file
hits. The corresponding original 1280×720 PNGs survived data-URL encode/decode byte for
byte. Evaluation and index paths were inaccessible through source reads. This exercises
source transport mechanically; the image blocks were not sent to a generating model.

Each named V2 row remains **FIT_UNVERIFIED**: actual image presentation to a generated
answer, the complete Host request profile and question-level online acquisition remain
unexercised. There was no text-only substitution or reduced-haystack claim.

Existing local vLLM discovery returned Qwen3.6-35B-A3B-FP8 with a 65,536 context; the local
configuration contains a vision architecture. This does not establish actual Reader
image understanding. No standalone compatibility generation was added. A0 Host prompts,
model-request ledgers and actual evidence-presentation traces are **NOT_RUN**, not ready
artifacts inferred from old experiments.

## Versions and provenance

| Asset | Pin |
| --- | --- |
| V1 code | `9e0b455f4ef0e2ab8f2e582289761153549043fc`, MIT |
| V1 dataset revision | `98d7416c24c778c2fee6e6f3006e7a073259d48f` |
| V1 Oracle file | `821a2034d219ab45846873dd14c14f12cfe7776e73527a483f9dac095d38620c`; matches remote LFS object |
| V2 code | `2cc8c540bdb87fe6761629b585e727e1c4704520`, Apache-2.0 |
| V2 dataset revision | `f152293e235517d504809563c833d7190b8c713b`, Apache-2.0 |

V2 questions, trajectories and Small membership match the official revision's checksum
file. Full file hashes, archive hashes and source mappings are in the raw evidence.
The V1 code checkout has pre-existing modifications; no official generation/evaluation
script was executed. The data format and nonstandard diagnostic boundary were checked
against the [V1 repository](https://github.com/xiaowu0162/LongMemEval) and
[V2 repository](https://github.com/xiaowu0162/LongMemEval-V2). No official accuracy or
[LAFS leaderboard](https://github.com/xiaowu0162/LongMemEval-V2/blob/main/leaderboard/README.md)
result is asserted.

The evaluator is a **user-authorized subagent**, replacing the requested human review.
This is model-assisted orchestration work, separate from benchmark Provider/Judge calls;
it is not an independent human gold label and its labor is not reported as free. No
model judge ran inside an experimental Host. The ten questions opened for qualification
are now exposed development material for future work, even though no answers were generated.

## Failures, verification and remaining work

The initial external metadata connection without the environment proxy timed out; normal
external networking then succeeded. Loopback discovery continued to bypass proxies.
The image checker initially required unavailable Pillow; it was changed to standard-library
PNG framing/checksum/decompression verification, avoiding an unrelated dependency install.
Neither failure consumed a benchmark generation or changed any source image.

The three new gate tests pass; the full applicable Lab suite has **601 passed**,
excluding the superseded projection prototype suites. Boundary, Ruff, mypy (35 source
files) and build pass. Exact final check results are recorded in the execution audit.
Historical v0.4/v0.5 experiments were not
rerun or rescored, and their contracts/results remain unchanged.

Raw evidence: `/cra/memory/mx_memory/evidence/v0211-external/x0-20260909/`.
Key files are `dataset-pins.json`, `candidate-manifest.json`, `evaluation/`,
`source-export-result.json`, `source-fidelity.json`, `position-ledger.json`,
`backend-fit.json`, `branch-log.json` and `admission-result.json`.
The independent mechanical supplement is in sibling `backend-20260909/`: `result.json`,
`mcp-calls.jsonl`, `source-interface-pagination.json`, `cleanup.json` and `execution-audit.json`.
The original admission artifacts retain their historical pre-service status.
No owned service or model job remains; source files and evidence are retained.

To complete an external behavioral diagnosis, a qualified question population and a
new prospective allocation/sequence are still needed. The current queue cannot supply
its required first wave. A revised partition must explicitly resolve old holdout
protection and tuning exposure; the later reviewed question cannot simply replace the
failed first slot. After that, actual A0 input/modality and per-question preflight and
sequential answer collection remain required. These are unfinished, not waived.

Keep 0.1.15/A0, eight tools and no automatic maintenance. No S1/S2 or strong binding
mechanism is justified from this admission stop. Product remains **0.1.x EXPERIMENTAL /
Implementation CANDIDATE / NO-GO FOR SCHEMA FREEZE**; no API, schema, permission,
Canonical implementation, migration or deployment changed.
