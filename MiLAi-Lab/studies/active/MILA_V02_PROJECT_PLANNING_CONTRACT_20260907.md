# 正常项目规划开发链：来源与评价准备

状态：`PREPARED_NOT_RUN`。这是Goal规定软件/既有项目规划家族的开发材料，不是留出集。
正式D3门仍未通过，因此本文件不启动正式D4或生成模型请求，也不封装可执行D5批次。
用户的本地完成优先授权保持；执行时需显式登记实际属于正式链还是独立开发条件。

使用MiLAi-Product现有完整项目文档作为来源：ADR-042（原文持久化）、ADR-044（连接
背压和未知提交）、ADR-047（可选租户准入）及MILA_REQUEST_TIMING_V1（观测边界）。
均按当前字节复制到独立准备目录，记录原路径/文件hash，不拼写未来题目到来源，不删首尾。
这些是文件观察，不能伪装成LME用户对话；后续应通过通用文件来源映射接入公开导入。
现有`v02_public_snapshot.py`硬编码LME source/session身份，不能直接改标签冒充已通用。

G的正常任务：

> 根据提供的项目文档，为MiLAi原文采集与Working State的发布前检查制定可执行的项目计划。
> 将详细计划写入details.md；如有需要交给下一会话继续的工作，写简短handoff.md，包含
> 当前进展、待验证问题和来源入口。保留原文件，不实际部署，不假设未观察到的测试通过。

G只接收上述任务和来源。它不知道下列续做问题，L1/L2由实际正常规划产生；没有候选产物
就记录无比较机会，不补摘要或追加G。Runtime不解释计划的业务字段。

A/B续做任务（只在G完成并冻结后交给冷会话）：

> 发布前演练留下三项观察：一次Working State写入请求超时，没有取得该操作的回执；新会话
> GET看到了更高的State版本。另一次Evidence采集遇到目录同步错误，磁盘上仍可读到原文。
> 运维还准备把可选租户上限设为2，并将多进程部署宣传为已保证租户公平。请基于项目计划
> 交付此次演练的处置说明：区分已知事实、未知结果、可执行的下一步，以及哪些发布结论
> 目前不能成立。不要实际重试写入、删除文件或修改服务。

首次实际使用边界为Host最终交付该说明；未交付草稿不自动计为实际操作，不允许事后
改名draft规避已发生的交付。只核验来源明确支持的必要条件：

1. GET更高head不足以确认超时操作成功或失败；操作确认与当前内容分开，无法关联时保留UNKNOWN。
2. 不建议盲目覆盖、自动重写或把当前内容相等当作该operation ID提交的证明。
3. 目录同步失败发生在Evidence/outbox提交前；原文存在不等于可确认的持久提交。
   原文可能是未引用残留，不能把本情形与Working State超时的未知提交混为一谈。
4. 保留完整来源和同步边界；后续显式重放需满足既有操作合同，不把处置说明视为删除/部署授权。
5. 上限2是每Database池/进程的可信tenant准入上限，不是多进程合计配额、任意租户公平或服务SLO。
   关闭此可选项不要求数据库迁移；不建议通过切换actor绕过限额。
6. 缺失计时保持未知；不把嵌套blob/数据库时段重复相加，也不凭接口或文件可读宣布模型效果/生产就绪。

以上条件需将首次交付逐条与冻结来源位置关联，不追加Judge模型；无法可靠判断的项记
未判定，其他文风、计划完备性或整体论证质量不自动PASS。开发者已知判据，明确为开放开发。

A/B拥有G同一完成点的所有正常文件、合法来源、公开Memory工具和保存能力，并独立分叉。
A预载同一L1＋L2，B只预载L1；保留同一Host策略/模型/宽松异常护栏和完整实际输入账本。
若复用当前CURRENT_TASK_ONCE，必须两组相同启用，并报告其额外呈现和费用；不称自主回查。

下一实现缺口：将普通文件的原身份/内容/时间与公开Evidence回执映射，独立核验三臂文件
字节和来源资格；不要用LME固定命名冒充新家族兼容。配置、Host源码、评价与来源hash冻结
完成后才允许登记新运行。当前准备目录不含模型凭据、执行开关或任何新模型分配。
