# V0221 HTTP-only 执行补充合同 v1

2026-09-11 用户重新要求执行 V0221，明确“使用vllm即可，不要用gpu”。
结合先前对路径 B、W2 及条件 W3 的明确授权，建立独立新批 `20260911-http-r1`。
不重启或改写旧 `20260911-r1`；旧未知用量原样承接，不结算为零。

该用户要求覆盖原 Goal §4.2 的直接设备检查方式：本批只允许 vLLM HTTP 查询/计数/推理，
禁止调用 Docker、nvidia-smi、NVML、CUDA 驱动、设备枚举或硬件控制。
不以旧诊断进程的设备错误推断运行中 vLLM 无法推理。

复用已冻结的安装代码 CPU 全矩阵及其源码/库指纹，标为 HISTORICAL_CPU_EVIDENCE，
不伪称本批重新核验安装进程/镜像。当前身份检查只通过 HTTP：模型、上下文、服务版本。
历史 WireSchemaAdmission 仅作为冻结证据验证器；实时 HTTP 身份检查单独进行。
HTTP 无法证明容器/镜像/所有启动参数未变化，报告中明确此证据边界。
真实兼容性仍必须通过 W2 全部16个计划请求验证，不把模型列表或健康接口当成真实生成通过。

其余合同保持：全部来源和合法对象、8对 canonical/wire、原完整输出验收、无自动修复，
W2 full8/finish8 两遍冷客户端，W3条件24条独立进程短链，每链≤4次。
历史承接只匹配两份指定旧账本及精确未知清单；任何新未知、违规或异常停止全批。
唯一批根、授权/代码/hash、跨进程批账、原子预约、永久停止及一次性启动防止新目录/重启重置。
W2≤16次/1800秒，W3≤96次/7200秒，单链300秒，HTTP≤60秒，输出4096，context65536，
累计raw cap=null，模型并发1。完整 wire 请求及全部参考轨迹先通过实际服务 tokenize。
不扩任务、不开发 State、不改 Product/共享服务、不打开保护池、不追加 Judge。

首个失败保留证据并停止；阶段未触发不填成已测试。此补充合同不授权第二个真实候选。
