# V0222 用户指令驱动的分阶段执行补充

用户最新要求执行 V0222 Goal，之后执行后续 Memory 再准入规划，并以 subagent 代替人工授权、
标注和审查。授权来源是这条用户指令；subagent 负责独立审核具体冻结实例是否落在授权范围内，
不能自造用户同意、忽略失败或预填能力通过门。

因此原文的“仅交付文档 / 当前分配 0”保留为成文时状态，不阻止这次明确要求的实施与执行。
本批 `evidence/v0222/20260911-http-r1` 绑定 P1≤24、P3≤16、P4≤96；每阶段及条件触发、
顺序、单候选、逐请求/阶段时间和首次异常停止合同保持。授权容器时间窗36小时不延长各阶段
1800/1800/7200秒或链300秒限制。模型并发1、输出4096、累计raw cap=null。

P1 的原定内容差异是诊断观测，可在HTTP/usage可靠时按冻结顺序继续；不改变旧验收逻辑，
不派发业务。任一新的未知费用、HTTP异常或证据/授权/身份漂移仍停全批，不能转入历史白名单。
精确承接 V0220两账和V0221最新已停账：已知30085raw、唯一旧未知预约28284、总实际未知。
旧批不续跑；新阶段不可改目录重置额度。P2修复必须由P1预先冻结的选择器选中。

后续E1/E2/M0/M1按路线中明确的条件上界逐阶段建立新合同、任务/标注/判据和独立subagent审查。
在前置门未通过时不得发送，也不得把“准备路线”当作已完成后续实验。提示分支尚无具体在线
矩阵和上界，必须先交付独立有界合同及明确范围审查，不能使用P1/P3剩余额度偷偷试提示。

全部推理/模型服务验证只使用vLLM HTTP。禁止直接设备、驱动、容器调用及服务变更；
不读取或展示HF凭据，不改Product/A0，不开放保护池，不预选Memory机制。
独立标注保留分歧/UNKNOWN，不以subagent意见冒充客观真值或人类标注。
