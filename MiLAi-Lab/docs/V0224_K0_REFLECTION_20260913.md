# K0 执行准备反思

Observation：真实历史输入组合已复算并由 subagent 独立核对：6,491 files / 350,728,694 bytes，57 ledgers / 225 行，16 P3 / 24 P4 / 80 P4 references。
旧源码也被历史依赖图 pin，因此直接改旧计量器或旧 root 常量会使历史证据失效。
新模块开发后重新核验全部 6,491 个原输入，未发现 hash 改变。

Earliest layer：目前尚在 K0。没有发起计时实例。审查发现新夹具 seal 时可重新接受 scope.close 后变化的 bytes，以及 canonical 路径核验晚于写入；两项均已修复并增加局部反例。
另发现两次裸 `_operation(PREP)` 不具备原 `prepare_p4 → Session.run` 的起点、初始化与 60 秒截止语义，不能充当 K1 的实际路径。

Alternatives：这是测量合同与工程路径覆盖问题，尚无新的 CPU 性能结果，更无模型、自然任务或 Memory 失败证据。
旧 profiler 可能有高扰动，但不能用其小型对照比例估算实际准入性能。

Next test：先保留原 Session 及 guard 调用路径，明确诊断前缀截断点与真实截止，核验相同初态和 U/S 输出/检查覆盖。
补齐父进程 start→exit/报告成本、六进程固定顺序和完整 K0 冻结后，才执行 U/S 校准。

Change：采用新 V0223 文件保存 root binding 与 coarse observation，不改任何原 pinned 实现。
对新 fixture 加入写入前 canonical/fresh 检查、scope 内冻结源码与输入 pins、seal 后精确比较。
裸准入 pair 仅保留为待修开发原型，不准通过合同状态字符串使其冒充原 Session 实验。

Invalidation：当前没有新 Gate 证书可失效；旧 V0222 仍是原停止终态。root/observer 局部测试不代替 A1–A4。
原型改变后须重做相关局部测试及 K0 源码 hash；完整候选冻结后再按合同运行工程全回归。

Decision：继续修复和冻结 K0；研究模型/HTTP allocation 仍为 0，不开 B–E，不缩减总 Goal。
下一测量资源仍按 V0223 v0.1：六个新进程，U/S、S/U、U/S，单进程外层 300 秒，原参考 Session 60 秒，S/U 中位比 ≤1.10。

证据：[K0 原始输入](/cra/memory/mx_memory/evidence/v0224/20260913-a-k0/input-inventory.json)、
[独立复核](/cra/memory/mx_memory/evidence/v0224/20260913-a-k0/independent-inventory-review.json)、
[原输入保留核验](/cra/memory/mx_memory/evidence/v0224/20260913-a-k0/post-development-original-input-check.json)、
[尚未准入的合同草案](/cra/memory/mx_memory/evidence/v0224/20260913-a-k0/contract-draft.json)。

后续落实：已将裸准入原型替换为原 `prepare_p4 → Session.run` 的首次generate前缀，以及原materializer的55scope前缀。原deadline与真实失败stop保留，诊断截断仅标PREFIX_REACHED_NOT_REFERENCE_PASS。
K0 setup r1在seal前暴露CPU manifest仅有传递控制引用，不能直接索引public leaf；修为经原PRESENTATION_BINDING→manifest查pin，5项局部测试通过，原失败保留。新root尚未创建时修订setup尝试，不恢复任何停止实例。

## v2：严格 U 观测修订

最终 v1 K0 审查发现 helper 在 U 中记录 guard 与 Session 内部观测时间，违反 v0.1 的 U 最外层计时口径。该问题在任何 K1 启动前发现；七套 v1 fixture/inventory 与拒绝审查保留，不算性能失败或有效校准。

v2 关闭 U 的 helper 内部观测时钟，保留业务 Session 建立及检查 deadline 的原时钟、原 60 秒期限、非计时覆盖记录和全部验证路径；S 继续 coarse 计量。新增 v2 文件和新 root/episode IDs，已封 v1 与 V0222 源码不修改。局部严格 U 测试 12 PASS，编排测试 13 PASS；正式证据仍须新 K0 七套实际闭包冻结和六进程开销校准。此修订不改变 1.10 开销门或 K2/K3 工作负载。
