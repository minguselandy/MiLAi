# v10 小规模结果与未完成项

**B–D 工程已实现并通过窄检查；E1 已完成，决策依据在真实轨迹中未被采用，完整 Goal 尚未完成。** 本次发布保存全部开发、原生失败和费用，不将接口存在或 Host 终结视为方法有效。ordinary 继续作为默认配置。

开发、语法与参考核查见[执行记录](CONTEXTUAL_USER_MEMORY_V10_DEVELOPMENT_20260925.md)。机器可核对的数值、原始制品路径和 SHA 见[结果清单](../data/manifests/contextual-memory-v10-results.json)，选择边界见[小样本清单](../data/manifests/contextual-memory-v10-selection.json)。原始轨迹、模型和语料保持 ignored。

## 相同条件下的完整原生 arc

冻结 47 个运行／配置文件，映射 SHA-256 为 `57fef8a534cef57e2d44015f91bb6fb443066484ca1420f74c4216a655344f14`。两臂均从原始空记忆和业务世界开始，完整执行既有 MERIT `arc0-000` 的 5 个 episode、7 条公开消息；原 session、到达顺序、工具和 checker 不变。运行前后源码与配置核对一致。

Host 为 Qwen3.6-35B-A3B-FP8，经 vLLM，thinking=true、temperature=0、每次输出上限 4096、每轮最多 12 次、上下文 65536；embedding 为 bge-m3、1024 维、batch 32。两臂均 state_policy=off，其他容量与日志／恢复保障相同，单控制器并发 1。notes 先运行、basis 后运行；只有一个已暴露 arc 和每臂一次轨迹，不能据此推断一般质量或稳定因果效应。

| 指标 | 强工作笔记 notes | 单决策依据 basis |
| --- | ---: | ---: |
| 原生通过 | 5/5 | 3/5 |
| 依赖任务通过 | 2/2 | 1/2 |
| Host 轮次 complete | 7/7 | 7/7 |
| 程序维护待办未清的 episode | 0 | 0 |
| 生成请求 | 25 | 26 |
| 输入 tokens | 191126 | 205492 |
| 输出 tokens | 29058 | 26980 |
| generation tokens | 220184 | 232472 |
| embedding 请求／tokens | 11／1789 | 11／2166 |
| embedding cache hits | 0 | 0 |
| 截断生成 | 1 | 1 |
| 写入／finish 合同拒绝 | 0／0 | 0／0 |
| 实际工作笔记更新 | 15 | 不适用 |
| 实际决策依据更新／采用 | 不适用 | 0／0 |
| critical_gap 实际查询 | 不适用 | 0 |

basis 的 generation tokens 在这次轨迹中多 5.58%，原生分数更低；不能把差异独占归因为准确采用、自动通知或缺口检索，因为这些机制没有实际使用。

## 实际失败与存储状态

两臂各有一次输出截断，程序未执行部分工具，下一正常调用继续；两次费用全部保留。

basis episode 3 先读取订单，再查询不存在的 `refund_policy`，随后读取真实退款窗口和人工审批阈值。它没有执行退款，原生 checker 判失败；回答还声称已提交人工审核，但没有对应业务工具或回执。原生结果不等于对现实退款审批规则的普遍判断，未执行的人工审核声明则明确缺少动作支持。

basis episode 4 的当前记忆正确保存并交付了 **6595 cents**，Host 却向 `refund` 传入 **659**。业务工具成功执行了错误金额，Host 也按该金额汇报；这是可见材料到动作之间的语义错误，不是工具拒绝或检索未找到。该事项卡仍在 revision 2，内容仍为待处理，退款后未修订。notes 则执行 6595，并把该原卡更新至 revision 3 的已处理状态。因此程序维护终结与语义维护正确必须分开报告。

## 决策机制机会与定位

basis 的 25 个完整动作都生成 `state_delta:null`，另一次输出截断；没有 set／clear 提案，也没有因状态字段或证据句柄而拒绝的调用。请求实际包含新外壳 schema、当前决策投影和使用说明；当前决策始终为 null。notes 则实际更新工作笔记 15 次，另有 9 次 null。

准确采用 → 变化 → 重核 → 后续使用链 **未建立**。旧记忆金额变化横跨原始 episode，而任务切换按合同清空当前决策；同 episode 的两条消息又涉及不同订单。Host 本可在同轮采用并修改旧卡，但这次没有声明采用。代码路径及缓存的确定性检查通过，不能补作真实语义链证据；也不能从 null 推断 Host 已充分评估新观察。

本次观察能确认 Host 没有使用候选，不能区分任务是否无需该表示、提示是否不足以促使使用，以及模型对复杂外壳的偏好。没有强制先写 State、修改题目、合并 session、添加问题或用新一轮通过覆盖失败。

| 后续项 | 状态与原因 |
| --- | --- |
| E2 缺口取材 | NOT_RUN；E1 没有实际依据或 gap query，缺少可用对照前态 |
| E3 自动重核 | NOT_RUN；没有采用集合或真实通知可供比较 |
| E4 持续维护 | NOT_RUN；原生窗口缺少同一决策持续复用机会，不扩造 N=4/8 |
| StateMemBench | NOT_RUN；本次未取得可核验的官方数据、许可与 scorer |
| 文档四轮 | 未重复运行；不为增加通过数量重复已有消费者序列，不宣称 v10 文档实测通过 |
| 全量套件／benchmark／Product 迁移 | 未运行，范围外 |

## 完整费用

v10 连续账本为 **51 次生成／452656 generation tokens／3955 embedding tokens**，unknown=0，LLM Judge=0。摄入通过既有来源接收完成，没有额外摄入或 State 模型调用；embedding 的实际请求全部计入。原生 checker 是本地代码。

下表按一次响应最终选择的主要动作分组；每格为输入／输出／合计 tokens。状态与动作同次推理，重复注入状态也已经进入输入费用，不能精确拆出独立的边际 State 成本。

| 主要动作分组 | notes | basis |
| --- | --- | --- |
| 业务动作 | 75980／6616／82596 | 92529／9697／102226 |
| 持久记忆维护 | 47419／14235／61654 | 42691／9043／51734 |
| 终结回答 | 60600／4111／64711 | 62327／4144／66471 |
| 截断 | 7127／4096／11223 | 7945／4096／12041 |

旧 v9 账本保持 119 次生成／966332 generation tokens／4358 embedding tokens，SHA 未变化；没有把旧费用清零或计为本轮增量。

## 交接

Lab-only；新增的是研究任务状态与动作外壳，未修改 Product API／Schema、权限或 Canonical 行为。原 write／material／operation 合同延续，旧 State 和 H1–H6 保持冻结。没有移动包边界，不运行额外边界门禁或构建。回退基线为 `7247742133de888276342c48638b12c81eaeec23`；新 checkpoint 使用 method v15，不能伪装旧 v9 身份载入。

[复现说明](CONTEXTUAL_USER_MEMORY_V10_REPRODUCE.md)提供真实源码准备与运行入口。当前工程可提交发布，但 Goal 的真实采用／反馈链和方法收益仍未满足，保持 active；不得把本次提交标成完整 v10 研究结项或一般可靠性通过。
