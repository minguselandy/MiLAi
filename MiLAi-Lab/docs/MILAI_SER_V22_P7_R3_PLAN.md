# P7 R3：只在存在记忆证据或失效派生文本时加入authority

R2检查点`12e654f4ab941dc70ce8ef0a16d63000dbe25b77`已由Luna发布并核对remote。真实失败隔离成立，Formation/Reconciliation仍有独立缺口。本轮唯一方法变化是stage v21的authority条件：`projected.items or projected.derived_rebases`。CURRENT、UNKNOWN、隔离/刷新项及assistant-only stale保留原规则；空history/空search回到B1原系统合同。v19/v20旧stage仍维持原无条件行为。

不读case、问题关键词、gold或金额；不改变search策略、记忆工具schema、action schema、refresh cap、lineage、business loop、R2局部容量策略或vLLM。复用R2 config，新R3源码锁绑定provider与必要CLI默认路径；不再复制config。

离线反例先验证完整wire：空证据与B1相同；CURRENT/UNKNOWN有authority；items空但derived_rebases非空仍有authority；历史stage行为保留。仅跑受影响窄测试/static与一次包含新锁的必要build。

然后新空namespace串行运行A5原十二例和原MERIT arc0。固定比较参考在调用前声明：B1诊断用R1原8/12轨迹，B1 MERIT用R2原4/5、dependent1/2轨迹。这两条源标签保留，不能称同源新matched运行或重挑最好B1；B1路径没有改变，以离线wire等价核对支持参考复用。P9正式评价另用同源B1/lite/full，不用本轮比较代替。

选择只重跑受影响A5是为了验证通用修复和原任务覆盖，不是寻找最高分。沿用原输入、rubric、world、业务checker和次序；所有失败、null、skipped消息和费用继续保留。d11是否自然搜索仅作描述：即使相同首请求出现不同答案，也不继续靠prompt调到通过。当前/冲突/派生风险协议未变的证据与P6十三例历史结果分开，不将旧运行重标R3。

验收关注机械合同恢复和无证据实际开销，完整评审原任务及未解决断点。无自然stale时SER效果分母仍为undefined。若再次出现语义退化，先核对实际wire与形成/检索链，判断是否为本方法变化，而非反复重跑筛结果。

Root控制真实调用并发1；起点360生成/332460tokens/3902embeddingtokens/75exact reads，使用原SER账本。源码/参数/锁冻结后才调用。完成后按总计划§25九条判定P8，明确已证明的机制和未证明的端到端收益，再继续P9–P12；不以本轮发布结束master。
